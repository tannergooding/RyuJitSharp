# Port findings and post-port work

Preserve actionable findings without turning the clean port into a rewrite.
Record a stable ID, category, location/evidence, impact, proposed action, and
disposition. Distinguish confirmed defects from hypotheses.

Fix translation defects that block correctness or parity in the relevant batch.
Record suspected upstream bugs and ask before introducing an intentional C#-only
behavior change. Defer unrelated renames, refactoring, and restructuring. Link
existing deviation entries rather than maintaining competing descriptions.

| ID | Category | Evidence and impact | Proposed action | Disposition |
| --- | --- | --- | --- | --- |
| B001 | Port correctness | [R002](DEVIATIONS.md#r002-windows-dump-line-endings): Windows output paths produced different raw line endings. | Centralize host newline encoding; preserve native message layout and check embedded/fragmented writes. | Corrected; 16 focused cases in Debug/Release and raw before/after dump comparison. Unix execution remains unverified. |
| B002 | Debugging design | [R001](DEVIATIONS.md#r001-temporary-serialization-for-debugging): `CILJit.compileMethod` serializes all compilations for debugger usability. | Remove, exclude, or make debugging serialization opt-in when useful; verify concurrency before claiming support. | Deferred until needed; not a permanent design requirement. |
| B003 | Tooling maintenance | `GenerateTables.Program.Main` uses working-directory-relative inputs and deletes its relative `Outputs` directory. | Consider explicit input/output paths and a reproducibility check when generator maintenance requires it; retain native input provenance. | Candidate, not a prerequisite for the clean port. |
| B004 | Port correctness | `Compiler.compInitOptions` initialized `verboseDump` to `true`, unlike native `compiler.cpp`. A six-method corpus produced 719 compilation headers, including 713 unselected methods. | Restore the native `false` default and reject unselected headers in the comparison runner. | Corrected; the runner rejects the old binary's 713 extra headers and accepts exactly six with the fix. |
| B005 | Port correctness / upstream synchronization | `Globals.dumpSingleInstr` formatted `ShortInlineI` as `sbyte`, so old native `ldc.i4.s -2` rendered `0xFFFFFFFE` but C# rendered `0xFE`. Target native commit `6276bd4a23e` fixes the native format from `%X` to `%llX`, making the target output `0xFFFFFFFFFFFFFFFE`. | Sign-extend both short and 32-bit signed integer operands to `long` before formatting; retain full-width `InlineI8`. | Corrected; focused boundary cases and the target-host Main dump match the target integer spelling. |
| B006 | Dump fidelity | Native floating operands use `%f`, whereas the port used default floating formatting. The pinned native dumper probe confirms six fractional digits, widening of single operands, signed zero, and Windows CRT spellings including `-nan(ind)` and `nan(snan)`. The suspected 64-bit integer truncation was an upstream bug already fixed by the target revision; see B005. | Match finite formatting with invariant culture and preserve host-specific non-finite spellings. | Corrected; focused managed cases match the isolated pinned native function probe, including under a non-English managed culture. Unix execution and full pipeline parity remain unverified. |
| B007 | Port correctness | `Globals.dumpSingleInstr` computed the byte-display length before advancing over variable-length switch/phi operands. A two-target switch displayed only its opcode instead of all 13 bytes. | Compute display length after consuming the operand table; retain the native returned instruction length. | Corrected; empty/nonempty switch cases and returned lengths pass, with the nonempty output matched to the native probe. |
| B008 | Port correctness | `Compiler.info` was implicitly zero-initialized, bypassing the `Compiler.Info` struct constructor and its field initializers. This lost `BAD_VAR_NUM`, empty-array, and other defaults; Main's first outgoing-argument temp was incorrectly named `FramesRoot`. | Explicitly construct the embedded `Info`. Watch for this C# value-type initialization trap when porting native aggregate members. | Corrected; target-host capture now shows native `V00 tmp0`, not `V00 FramesRoot`. |
| B009 | Port prerequisite / fidelity | The new unpinning phase depends on local threading that the existing `fgLocalMorph` stub does not establish. The initial supporting DFS implementation also omitted preorder/postorder assignments and cycle state. | Complete native DFS contracts and local-threading prerequisites before exercising pinned/EH cases or claiming the new optimization is usable. | DFS/tree/EH support corrected; six focused graph cases pass in Debug/Release. Local morph remains a known dependency on preserved WIP. Runtime captures still have neither EH nor pinned locals. |
| B010 | Upstream semantic synchronization | The helper-property table still classified `CORINFO_HELP_GETCURRENTMANAGEDTHREADID` as pure. Target `utils.cpp` removes purity because runtime async methods can resume on another thread. | Remove `IsPure` while retaining the native no-throw classification; preserve the other updated helper flags. | Corrected; the targeted test failed before the flag change and all three updated-helper cases pass in Debug/Release afterward. |
| B011 | Dump fidelity | `BasicBlock.dspFlags` always prefixed flags with a space unless `BBF_IMPORTED` was set, unlike the native first-displayed-flag rule. | Track the first displayed flag, retaining native order and the target's new `stale-pred` label. | Corrected; three cases reproduced missing/extra output before the change. |
| B012 | Inline-scan fidelity | Foldable intrinsic observations were followed by an unconditional unknown-stack push and an extra generic intrinsic observation. Unsafe binary operations also reused helpers with different native observation contracts. | Preserve the native intrinsic-specific stack updates and observation ordering while reconciling named-intrinsic resolution. | Corrected with the `fgbasic.cpp` batch; focused intrinsic-stack, tail-prefix, and EH-end regressions pass. Removing the stack/tail/EH fixes produces seven expected failures. |
| B013 | Generator source drift | Non-hardware `NamedIntrinsic` declarations were hardcoded in `GenerateTables`, so refreshing HWI inputs did not incorporate upstream additions or preserve target enum ordinals. | Derive the enum body from pinned `namedintrinsiclist.h` and expand the hardware list through the existing parser. | Corrected; all identities and ordinals match native preprocessing for x64, x86, ARM64, RISC-V64, and Wasm. All 34 generated outputs match maintained destinations; repeat generation is deterministic. |
| B014 | Target compilation | Wasm compile attempts hit existing syntax/constant errors in `DoNotEnregisterReason.cs`, `GenTreeIntConCommon.cs`, and `Compiler.Options.cs` before validating the synchronized target definitions. | Repair these in a dedicated target-enablement batch and rerun the full target build; do not infer Wasm support from x64 builds. | Recorded; no Wasm compilation or execution parity claimed. |
| B015 | Port prerequisite | `ExtendedDefaultPolicy` was an empty subclass, including in saved WIP, while the target's new `AsyncStressPolicy` relies on its precise observations and heuristics. | Port the complete extended policy and trusted-profile predicate before adding async stress; retain unsigned arithmetic, folding estimates, profile/local-capacity scaling, and diagnostic order. | Extended and async stress policies, factory selection, callsite-index forwarding, and native grouping helper are implemented. Focused tests cover deferred limits, shuffled indices, depth decay, and random-stream consumption. `fgInvokeInlineeCompiler` now prepares the inlinee group after the last rejection point; the grouping hook in `fgInline` remains unported. Do not claim end-to-end async inlining. `DiscretionaryPolicy` and `ProfilePolicy` remain shells. |
| B016 | Dump fidelity | `DefaultPolicy.OnDumpXml` inferred C# field/property expressions as XML attribute names instead of native `m_*` names; shared numeric attribute formatting used the current culture. | Preserve explicit native attribute names and invariant CRT-compatible numeric formatting, sharing the IL float formatter with policy diagnostics. | Corrected for DefaultPolicy and ExtendedDefaultPolicy; an exact-output case under `fr-FR` passes. This does not establish full inline-tree XML parity. |
| B017 | Suspected upstream diagnostic bug | Native `ExtendedDefaultPolicy.DetermineMultiplier` prints `m_ArgUnboxExact` in the message for non-exact argument unboxes even though the branch is controlled by `m_ArgUnbox`. | Consider an upstream diagnostic correction separately; do not silently change the managed message's argument. | Native behavior preserved in the clean port. |
| B018 | Random-sequence fidelity | `InlineStrategy.CreateRandom` used `System.Random`, which differs from pinned `CLRRandom` for `int.MinValue` seeds and negative-range rounding. | Port the seeded native generator and retain seed precedence, method-hash XOR, first-call caching, and shared shuffle/profitability consumption order. | Corrected; all 77 native sequence cases match in Debug/Release, including `NextDouble` bits and interleaved calls. Unseeded Windows initialization uses native clock/thread/process inputs but is nondeterministic; non-Windows unseeded initialization remains explicitly unsupported. |
| B019 | Managed representation correctness | `GenTreeCall._Anonymous1_e__Union` overlaid `AsyncCallInfo`'s managed debug-context reference with non-object fields. Constructing a call node threw `TypeLoadException`; three async grouping tests reproduced it. | Give the mutually exclusive variants separate managed storage, retaining flag-based selection and whole-storage copying. | Corrected under D002. Grouping passes, and a Debug/Release regression confirms copied call storage preserves its managed debug-context reference. This is not a native interop-layout change. |
| B020 | Port correctness | The two-argument `gtPeelOffsets` still accumulated field sequences through `out _`, unlike native's null output pointer. After peeling, `fgAddrCouldBeHeap` used a stale cached operator and misclassified stack/return-buffer addresses with offsets. | Skip unused field-sequence work and inspect the peeled node's current operator, reusing native field-address peeling. | Corrected; six direct/offset address cases pass in Debug/Release. Before correction, offset cases first asserted on the unused field-sequence store and then exposed the stale-operator result. |
| B021 | Port correctness / upstream synchronization | `getBaseTypeAndSizeOfSimdType` indexed past C# string ends while looking for native NUL terminators in `Vector`, `Vector2/3/4`, and `Vector<T>` names. Target native also rejects cross-target `Vector<T>` sizes that disagree with the VM. | Use managed length checks, preserve target size rejection and diagnostics, and remove the obsolete write-only SIMD handle cache. | Corrected; 15 metadata cases pass in Debug/Release. Restoring the Vector2 terminator access and removing the size guard produces one bounds failure and two wrong-size failures. These are controlled EE metadata tests, not runtime SIMD execution parity. |
| B022 | Intrinsic lookup fidelity | Generated portable vector IDs share one ISA range, while later codegen-only IDs repeat ISA groups. Existing lookup ranges could include those unsorted pseudo IDs or miss portable-width aliases. | Match native searchable FIRST/LAST ranges and shared vector aliases; return `NI_Illegal` for absent ISA groups. | Corrected; 13 managed lookup probes pass and native range/sort audits cover xarch, ARM64, and Wasm metadata. Intrinsic importer/backend execution remains unported. |
| B023 | Port correctness | `fgMeasureIR` constructed a visitor for each statement and replaced the accumulated count, losing nodes from earlier statements and blocks. | Keep one visitor across the traversal and add its total to the separately accumulated LIR count. | Corrected; three cases pass in Debug/Release. The two multiple-statement/block cases fail with the previous implementation. |
| B024 | Importer fidelity | UTF-16 string/span comparison expansion accepted nonconstant comparison modes as ordinal. Discarded span inline-candidate cleanup replaced only a local reference, leaving the `GT_RET_EXPR` candidate unchanged. | Reject nonconstant modes and replace the stored candidate; retain target argument-pop-before-store ordering and spill checks. | Corrected alongside the target vectorization changes. Rejection probes preserve stack depth; successful EE-backed expansion remains unexercised. |
| B025 | Upstream semantic synchronization | GDV candidate marking discarded every non-inlineable target and reported the original call target to the EE rather than the actual guarded/unboxed target. `GetGdvCandidateInfo` also incorrectly required the call to be an inline candidate. | Preserve target retention restrictions, report the selected callee, and distinguish `isInlineable` from GDV candidacy throughout candidate marking. Remove obsolete candidate context-lookup state and its producer arguments together. | Corrected; eight retention/callee cases fail before and pass in Debug/Release afterward, including multiple candidates and all four retention exclusions. GDV expansion and end-to-end codegen remain unported. |
| B026 | Port correctness | `impNormStructType` overwrote a successfully recognized SIMD base type with `TYP_UNDEF` before returning. | Initialize the output before recognition and use the target's shared `structMightRepresentSimdType` predicate. | Corrected; four recognized-vector cases reproduce the lost output before correction. All five recognized/unrecognized metadata cases pass in Debug/Release afterward. |
| B027 | Upstream semantic synchronization | Importer statement appending ignored ordering-only effects when spilling earlier global reads; field-store annotation dropped global reads from the stored value; inline null-check elimination ignored local stores observable by protecting try/filter regions. | Preserve `GTF_ALL_EFFECT`, stored-value global effects, and native `HasPotentialEHSuccs` visibility across additional trees, arguments, statements, and the import stack. | Corrected; 22 focused IR cases pass in Debug/Release, including 11 that fail against the previous implementations. These checks do not establish runtime exception-ordering or generated-code parity. |
| B028 | Upstream semantic synchronization | NativeAOT generic virtual lookup now passes a dispatch cell, but `impImportLdvirtftn` still tagged it as a method handle. Wasm also cannot use the ReadyToRun dynamic-helper fast path. | Use the target's function-address handle kind and exclude Wasm from that ReadyToRun branch. | Corrected; four direct/indirect and class/interface lookup cases fail before and pass in Debug/Release afterward. NativeAOT call codegen and Wasm execution remain unverified. |
| B029 | Upstream semantic synchronization | `impImportNewObjArray` grew a reused dimensions local, invalidating the layout against which earlier full-width stores were created. | Allocate a new local when more dimension storage is needed; retain reuse for equal or smaller requests. | Corrected; the growth case reproduces the changed earlier layout and incorrect reuse. All three growth/shrink/equal-size IR cases pass in Debug/Release afterward. |
| B030 | Upstream semantic synchronization | The minopts boxing temp-sharing exception tested `isEnum != Must`, so known enum boxes missed the single-definition locals intended to help `Enum.HasFlag` optimization. | Apply the target's `isEnum == Must` predicate without changing the other sharing restrictions. | Corrected; all three `Must`/`MustNot`/`May` cases fail before and pass in Debug/Release afterward. The fixture initializes the native local-tracking limit and confirms the EE enum query actually runs. |
| B031 | Diagnostic evaluation / maintenance | `Globals.JITDUMP(string)` checks `verbose` only after C# has evaluated the message. In Debug, `lvaSetClass` therefore queries `eeGetClassName` even with dumps disabled, unlike native `jit.h`'s guarded macro. | Consider a centrally guarded interpolated-string handler, or explicit guards where needed, preserving existing formatting and enabled-dump behavior. | Confirmed from the call contract and boxing fixture; deferred, not an accepted behavioral deviation. No performance impact has been measured. |
| B032 | Upstream semantic synchronization | `impFixupStructReturnType` only materialized a pseudo return buffer for direct calls, missing an inline candidate wrapped by `GT_RET_EXPR`. | Inspect the placeholder's candidate call and retain the import-time no-substitution invariant. | Corrected; the placeholder/return-buffer case fails before correction. All four direct/placeholder and buffered/unbuffered IR cases pass in Debug/Release afterward. |
| B033 | Port correctness | `impTailCallRetTypeCompatible` tested the caller's managed convention twice, permitting implicit return widening when the callee uses an unmanaged convention. | Require both calling conventions to be managed, matching native. | Corrected; the managed-to-C widening case fails before correction. Five calling-convention/widening cases pass in Debug/Release afterward. This is ABI eligibility coverage, not generated tailcall execution. |
| B034 | Upstream semantic synchronization | `impInheritAsyncContextsFromInliner` copied only execution/synchronization contexts, losing resumed use/def, enclosing frames, operand effects, and shared frame-handling metadata. | Inherit the complete context chain in native argument order, preserving the inlining call's frame-handling identity. | Corrected; three zero/one/two-outer-frame cases fail before correction. Those and two no-inheritance cases pass in Debug/Release. SaveAsyncContexts is now ported; general inline expansion and async lowering remain unported dependencies. |
| B035 | Upstream semantic synchronization | `gtNewCallNode` omitted the call's own exception flag; call import propagated only global effects from function pointers, hidden arguments, and receivers, dropping ordering effects such as volatile pointer reads. | Initialize call exceptions before attaching operands and propagate `GTF_ALL_EFFECT` at the corresponding import sites. | Corrected; four of six constructor/indirect-call cases fail before correction and all pass in Debug/Release afterward. Other main call-import changes remain pending. |
| B036 | Upstream semantic synchronization | Effect recomputation retained stale ordering flags from removed operands. Division/remainder nodes also lacked ordering support needed to remain below checks that prove them safe. | Clear unsupported operation-level ordering before accumulating operand effects, and support ordering on all four integer division/remainder operators. | Corrected; six of seven operation/operand-ordering cases fail before correction and all pass in Debug/Release afterward, including byref formation and restored volatile-argument effects. |
| B037 | Preserved-WIP integration | The preserved worktree's `Rationalizer.cs` constructs `RationalizeVisitor`, while its companion file defines nested `Rationalizer.Visitor`. The index version has empty call-rewrite bodies; restoring it would discard the more complete unstaged work. | Reconcile the worktree-only call rewrites and visitor name with their actual dependencies during WIP integration, without overwriting current compiler/graph fixes. | Reconciled and active for Windows x64, including call rewriting, sequencing, hardware support and parameter-register mappings. Twenty minopts methods complete rationalization; seventeen resulting IR sections match. Remaining hardware-import and EH-funclet differences precede this phase; other-target rationalization is explicitly deferred. |
| B038 | Upstream semantic synchronization | Main call import attached Memmove/SequenceEqual value-profile metadata too late for inline candidates and omitted optimized instrumented tiers; it could also duplicate profiled calls while collecting new profiles. | Collect metadata before wrapping inline candidates, reuse their shared metadata object, and suppress profile-driven duplication while instrumenting. | Source reconciled with the pinned target and integrated builds/corpus pass. Instrumented value-profile execution remains unexercised; the current corpus is not an instrumentation parity claim. |
| B039 | Upstream semantic synchronization | `impReturnInstruction` did not unwrap original task-valued IL returns for runtime-generated async versions, leaving the wrong stack value for the declared return ABI. | Port await wrapping/folding and its synthesized-call and monitor-release dependencies; preserve node identity when folding to NOP. | Implemented; four controlled-EE void/scalar and generic/non-generic return cases fail without the return hook. Nineteen return/support cases pass Debug/Release; NativeAOT and the seven-method host capture retain the known dump frontier. Struct/constructor folding, inline wrapping, synchronized async execution, prefix recognition, context producers and lowering are not covered by these results. |
| B040 | Port correctness / iterator reconciliation | `GenTreeUseEdgesList.Enumerator.MoveNext` dereferenced a null byref on empty/end enumeration and call transitions. The early/late cursor and reset state also did not preserve native traversal. | Reconcile the whole iterator, preserve writable edge identity and native order, and distinguish absent edges from null-valued optional operands. | Corrected; 25 of 28 structural cases fail against the preserved pre-fix assembly, and all pass in Debug/Release afterward. Coverage includes empty/bashed leaves, optional and reversed operands, linked phi/field uses, arrays, intrinsics, and early/late/control call order. ARM64-specific conditional cases are source-reconciled but not executed on that target. |
| B041 | Port correctness | `ConfigMethodRange.InitRanges` indexed a span after consuming its last character; `Contains(int)` compared signed hashes with unsigned endpoints, excluding high-bit method hashes. | Preserve native C-string end behavior with bounded span reads and compare the hash's unsigned 32-bit representation. | Corrected; 24 of 27 range cases fail against the preserved pre-fix managed assembly. All 27 pass both managed tests and a standalone probe using the pinned native function bodies, including high-bit/open ranges and delimiter/capacity cases. |
| B042 | Upstream bug / later cleanup | The pinned native `ConfigMethodRange.InitRanges` only updates `m_badChar` when it is already nonzero, so malformed, overflowing and truncated inputs can report no error. Delimiter-only input also creates a zero range despite the native header's whitespace claim. | Investigate/fix upstream and then synchronize; do not silently give the C# port different selection or diagnostic behavior. | Native probe confirms these quirks. The managed parser deliberately retains them for parity; comments and tests make the behavior explicit. |
| B043 | Upstream semantic synchronization | Task-await import omitted the native root-method/inline-ordinal/IL-offset range gate and repeated an obsolete async-thunk profitability query after token resolution. | Apply `impCheckOptimizeAwait` before committing to the async variant and let the EE's await resolution determine profitability. | Implemented; four debug hash-selection cases pass, including root-versus-inlinee identity and offset exclusion. Existing task-await fallback diagnostics now retain native leading newlines. Full async-version prefix production and runtime await execution remain pending. |
| B044 | Upstream semantic synchronization | Async-version tail prefixes had no producer; async calls lacked current tail/context ownership metadata, and call import rejected every tailcall in an async method before classifying the call. | Port the complete tail matcher, prefix/return cleanup, setup metadata and both ownership consumers; preserve unconditional continuation-argument insertion while gating only inherited contexts. | Implemented; 22 cases cover bounded patterns, EE consent, metadata, protected-region recognition and owned/inherited contexts. SaveAsyncContexts now supplies the context/EH-ID producers. The corpus does not exercise runtime tail-await execution or covariant return cleanup; no async codegen parity is claimed. |
| B045 | Port correctness / recursive visitor | `IGenTreeVisitor<TSelf>.WalkTree` used tuple assignment on two byref locals when requesting reverse execution order, swapping actual operands rather than traversal references. | Rebind the traversal references without changing the tree. | Corrected; four default/reversed traversal and writable-use cases cover operand identity and replacement location. |
| B046 | Port correctness / EH iteration | `EHClauses.Enumerator.MoveNext` advanced from the first entry rather than the current entry, repeating the second clause indefinitely whenever at least two remained. | Advance the current writable reference and retain a stable terminal state; cover empty tables, suffixes, reset, and graph updates across multiple EH regions. | Corrected; three of six bounded iterator cases fail before correction and all pass afterward, along with both nested-EH split cases. Runtime EH parity remains unestablished. |
| B047 | Port correctness / switch metadata | The three-argument `BBswtDesc` constructor ignores `hasDefault`, unlike the pinned native constructor in `block.h`. | Preserve the default-case flag when reconciling switch descriptor construction. | Confirmed from source; deferred from block splitting, which transfers an existing descriptor without reconstructing it. The dominant-case constructor does initialize the flag. |
| B048 | Port correctness / recursive visitor | `IGenTreeVisitor<TSelf>.WalkTree` combines the abort and operand-null conditions, then checks null-operand legality in the `else`; aborting a binary child can therefore trigger an unrelated Debug assertion. | Keep abort handling separate from null-operand validation, matching native traversal. | Corrected for binary and local-store unary traversal. Three split-tree cases reproduced the Debug assertion before correction; direct local-only visitor cases also cover both branches. |
| B049 | Upstream investigation / alias classification | Native `AliasSet::NodeInfo` records return-buffer local writes, then overwrites those flags when classifying call purity. Full alias-set construction also gates logical-definition visitation on the discarded local-write bit. | Check affected consumers, including LIR local-read validation and alias sets, before proposing an upstream correction. | Preserved in the restored C# classifier and complete alias set; not silently changed relative to native `sideeffects.cpp`. |
| B050 | Port correctness / local-store query | `TreeHasLocalStoreVisitor` compared a definition's own local number with its parent and promoted-field range rather than the queried local's descriptor. | Corrected using native logical definitions and the queried descriptor. | Whole/partial promoted stores, exact field matches, padding-only stores, early abort, signed overlap, and async definitions have 13 focused cases. Five promoted-range cases fail with the original query and pass with the correction. |
| B051 | Port correctness / composite SSA storage | `SsaNumInfo.Composite` captures the backing span before `CollectionsMarshal.SetCount`, then writes through the old span and copies using table indices instead of field indices. Growth can leave the span empty or stale. | Reconcile the outlined composite-name path with native before enabling SSA clients that use it. | Observed while porting definition-provider SSA accessors; compact encoding and the existing outlined implementation are unchanged by that port. |
| B052 | Upstream investigation / local-set emptiness | Native `LclVarSet::IsEmpty` returns true whenever there is no bit vector, including a nonempty single-local set. This affects `AliasSet::WritesAnyLocation` and the operand-read gate in its `NodeInfo` interference overload. | Investigate upstream before changing the clean port's behavior. | Preserved exactly; focused cases cover singleton, repeated insertion, expanded sets, intersections, clear/reuse, promoted definitions, and contained operand reads. |
| B053 | Preserved-WIP correctness / inline placeholders | Saved placeholder substitution allocated replacement NOPs for unused return expressions, self-stores and folded branches. Its postorder callback also lacked the target's child-to-parent effect propagation. | Bash the original nodes to NOP and propagate `GTF_ALL_EFFECT` after late devirtualization, preserving native identity, IDs and effect flags. | Corrected while restoring the walker. Seven cases fail with the saved replacement/effect behavior and pass afterward. Additional cases cover native-integer/byref retyping and prior inline-failure observations; branch cases retain extracted effects and predecessor edges. |
| B054 | Port prerequisite / inline folding | The folding dispatcher is incomplete, so folding-dependent substitutions and late optimization cannot yet fully match the oracle. | Finish nonconstant binary, conditional and HWI folding and required global-morph/VN support before enabling the complete inline phase. | Structural comparison, HWI initialization, special-intrinsic calls, scalar constant folding and floating constant-operand folding are implemented (B063/B081/B082/B084). Current importer and placeholder-walker callers consume scalar replacements. Integer-special, identical-operand comparison and conditional folding need global-morph assertion bookkeeping; HWI folding and value numbering also remain prerequisites. `fgInline` is unactivated. |
| B055 | Preserved-WIP correctness / tailcall recognition | Saved `EndsWithJmpMethod` and `EndsWithTailCall` read `_lastNode`, which is only populated for LIR, instead of the native tree/LIR-aware terminal accessor. Tree-form blocks therefore assert or dereference null, including during GC-safe-point cycle detection. | Use `BasicBlock.GetLastNode`, mapping native `lastNode()` without hiding the inherited LIR-only `LastNode` property. | Corrected; three tree-form JMP/fast/helper-tailcall cases fail with the saved lookup, while the three LIR cases pass. All six pass with the correction. |

| B056 | Preserved-WIP correctness / inline arguments | Saved single-use argument substitution searched the caller's LIR instead of rewriting unthreaded inlinee uses. Saved argument setup classified new pseudo-arguments as IL arguments, and unused struct-load cleanup dropped possible null faults (also using the binary-only managed `AsOp` accessor). | Rewrite live inlinee statement and detached-return edges under D002; use target `IsUserArg` classification and preserve faulting loads as null checks with the correct managed accessor. | Corrected; ten cases fail when replaying the saved replacement, classification and fault behavior. All 23 argument-integration cases pass in Debug/Release, including excluded substitutions, initialization, GC-local cleanup and null-check ordering. Full inline-phase/codegen parity remains unestablished. |

| B057 | Port prerequisite / symbolic continuation offsets | Generated operator metadata recognized `GT_CONTINUATION_MEMBER_OFFSET`, but `GenTreeVal` construction, expression cloning and leaf dumping did not. Five new async frame/load cases initially hit the constructor assertion. | Add the operator to the value-node constructor and cloning cases, and print its root-table index and member description exactly as native. | Corrected with continuation-member registration and async frame-return support. Focused coverage includes cloning, operand enumeration, cold-path CFG/EH structure, context arguments and four exact leaf dump strings. Continuation layout/lowering and complete inline-phase execution remain unported. |

| B058 | Preserved-WIP correctness / split offsets | Saved `fgSplitBlockAfterStatement` used signed `int.Min`/`int.Max`, but native `IL_OFFSET` is unsigned. A suffix without valid debug information therefore assigned different block boundaries for `BAD_IL_OFFSET`. | Preserve unsigned ordering locally, including the sentinel, while retaining the existing managed offset representation. | Corrected with statement splitting and root-offset lookup. Four cases cover empty blocks, empty/invalid suffixes, and nested inline debug locations. |
| B059 | Port correctness / EH insertion | `fgTryAddEHTableEntries` copied from the insertion index through the array's capacity, not the active clause count. Inserting into a table with spare capacity threw `ArgumentException`; two nested-inline cases reproduced it. | Bound both relocation spans by the active clause count, matching native `memcpy_s`/`memmove_s`. | Corrected with inline block splicing. Six cases cover outside/try/handler insertion with exact and spare capacity, stable IDs, enclosing links and block indices. |

| B060 | Preserved-WIP correctness / inline variable setup | Saved `impInlineInitVars` copied the inline-array local metadata by value and advanced the signature cursor before reading its first argument. It also omitted argument-temp sentinels and used an obsolete pseudo-argument exclusion list. | Write through a span over the actual metadata, advance signatures after each argument, initialize `BAD_VAR_NUM`, and use native `IsUserArg` classification with separate InstParam storage. | Corrected with argument recording and initialization. Six cases fail when replaying the saved table-copy/cursor behavior; all nine focused cases cover persistent types, pinning, receiver rejection, narrowing, pseudo-arguments, aliasing and side effects. Folding remains dependent on B054. |

| B061 | Port correctness / managed error traps | Managed callback exceptions escaped the unmanaged shim instead of producing the regular trap's failure result; handle release was not exception-safe. Restored inline invocation relies on recoverable JIT and EE failures. | Capture managed exceptions inside the callback, own its handle with `using`, and apply the native regular/SPMI-only and terminal-HRESULT policies after returning to managed code. | Corrected under D002. Ten cases cover success, fatal/ordinary/terminal managed exceptions, identity/stack-preserving propagation and EE rejection. A Windows-x64 NativeAOT probe also recovers a foreign SEH exception in a native handler across the managed callback, then collects and calls again. This is synthetic boundary coverage, not real EE-failure or successful nested-inline compilation evidence. |
| B062 | JIT/EE ABI correctness / boolean results | Function-pointer `bool` returns used four-byte BOOL marshalling, but native C++ bool defines only the low byte. An unoptimized native SEH trap returned false in AL with nonzero upper bits, and NativeAOT reported success. | Represent all 32 C++ bool vtable results as `byte`; normalize in the 89 inherited/public wrappers while retaining their managed bool APIs and native slot order. | Corrected. All affected declarations match C++ bool in the pinned headers. Two upper-register regression cases fail without the correction; all four cases cover static, dynamic and JIT interface wrappers. The native SEH probe now reports failure correctly without widening its C++ return type. |
| B063 | Port prerequisite / HWI node initialization | `GenTreeHWIntrinsic.Initialize` was a no-op, so construction did not set the requested intrinsic ID or native intrinsic-specific effects. `HWIntrinsicInfo.HasSpecialSideEffect` also remained stubbed. | Port initialization, ID/type normalization and required side-effect classification before enabling HWI folding or claiming correctly constructed intrinsic IR. | Corrected. Twenty-one cases cover identity, small signed/unsigned base types, loads/stores, fences/serialization, pause/prefetch, division, inherited effects, variable arity and setter behavior; 18 fail with the original constructor. Structural-comparison and iterator fixtures now use actual initialization and valid node IDs. HWI import/folding, codegen and ARM64 execution remain unverified. |
| B064 | Approved representation adaptation / whole-node replacement | Native `GenTree::BashToConst` changes an existing node into an integer/floating constant while preserving its address and aliases. Managed operator and constant nodes intentionally have different CLR classes and field/GC layouts. | Replace nodes wholesale and adapt call sites, owners, cached aliases and LIR links. Preserve semantic relationships, tree IDs/order, flags and value numbers without reinterpreting CLR layouts or redesigning nodes into mutable payloads. | Tree-form scalar replacements now preserve base metadata and logical IDs without consuming extra IDs (B082); current importer/walker uses consume their results. Constructors require unthreaded sources. Post-morph cached argument references, LIR position/users and lowering identity assertions remain unadapted. Overflow folding retains the native global-morph gate; VN support is explicit NYI (B083). The full inliner remains unactivated. |
| B065 | Port correctness / checked arithmetic | `CheckedOps.TryAdd`/`TrySub` returned overflow rather than success, their unsigned variants used signed comparisons with inverted success, and signed multiplication checked operand signs instead of whether the full product fits the signed result. Alignment inherited the addition defect. | Preserve the existing success/out-result API while matching native `CheckedOps::*Overflows` and `ClrSafeInt` arithmetic. Keep wrapped out-results even on failure. | Corrected. Thirty cases cover 32/64-bit signed/unsigned addition, subtraction, multiplication and alignment; arithmetic cases exhaust pairs from a boundary set against exact `BigInteger` results, including signed minima/maxima, the unsigned sign boundary, multiplication thresholds and zero times negative values. Twenty-eight cases fail before the correction. |
| B066 | Port prerequisite / checked casts | Folding and value numbering require the four `CheckedOps::CastFrom*Overflows` predicates and `FitsIn(var_types, T)` from native `utils.cpp`/`utils.h`. | Preserve signed/unsigned source interpretation, truncation toward zero, exact floating-point boundaries, NaN/infinity handling and rejection of unsupported destination types. | Ported to `CheckedOps.cs` and `utils/Globals.cs`; native definitions and declarations retired. Twenty-two cases check all eight integer destinations, single/double boundary neighbors, signed zero, subnormals, NaNs/infinities, floating destinations, invalid types and six signed/unsigned/native-sized source representations. Integer ranges and floating truncation are checked against `BigInteger`; no folding/codegen parity is implied. |

| B067 | Port prerequisite / profile counts | The first BBOPT dump difference came from the unported `fgIncorporateProfileData` phase. Its count-loading/scaling dependencies were absent, and `fgStressBBProf` incorrectly reduced the native unsigned seed to a boolean. | Port profile incorporation's complete dependencies before activating the phase; do not insert the missing diagnostic alone. Preserve counter widths, stress hashing, backedge subtraction, scaling and inconsistency metrics. | `fgGetProfileWeightForBasicBlock`, `fgIncorporateBlockCounts` and `fgApplyProfileScale` are ported and retired from the native ledger. Stress seeds now retain all 32 bits; two cases fail when the old boolean normalization is replayed. Fourteen Debug / eleven Release cases cover absent/missing/duplicate schema probes, unsigned counters, stress counts, zero/up/down scaling, backedges, profile flags and inconsistency accounting. B079/B080 complete phase activation and close the BBOPT dump gap. |

| B068 | Port prerequisite / profile spanning tree | Edge-count reconstruction and instrumentation require identical `WalkSpanningTree` traversal and edge classification. Ordinary DFS is insufficient: handler/filter roots, pseudo edges, callfinally continuations and cost-class ordering affect the profile schema. | Port the complete visitor contract and traversal before edge reconstruction; preserve queue-time marking, reversed successor snapshots and unreachable-block notifications. | Ported and retired from the native ledger. Sixteen cases verify exact callback sequences for ordinary/rare/critical forks, queued targets, duplicate switch slots, EH roots/exits, malformed catch returns, returning/retless callfinally blocks and minimal throw profiling, plus a multiword bit-vector graph with nonzero starting block IDs. Full profile incorporation remains unactivated. |

| B069 | Port prerequisite / sparse edge reconstruction | `fgIncorporateEdgeCounts` requires a separate flow model containing instrumentation pseudo-edges, not merely the CFG's successors. | Port preparation, spanning callbacks, conservation solving, OSR normalization, failure/repair decisions and dominant-switch screening together. Keep the ten-pass limit, reverse layout order, linked-edge order, unsigned counters, random draw order and exact diagnostics. | Ported with typed managed block-info/edge storage under D002. Twenty-six Debug / twenty-three Release cases cover counter widths, internal keys, diamonds, missing probes, negative counts, zero-entry repair, rejection paths, switch thresholds/defaults/duplicate cases, zero-flow likelihoods, OSR transfer/self-loop behavior and shared random state. Native quirks are retained: missing-target diagnostics print `ILOffset`, and `IsGood` indicates repairable count issues rather than whether data was discarded. Profile synthesis still gates full phase activation; no new native phase-parity claim. |
| B070 | Port correctness / switch default metadata | The three-argument `BBswtDesc` constructor ignored `hasDefault`. Import-created switches consequently lost their default marker, including when profile reconstruction screened dominant cases. | Initialize the default marker just as the existing four-argument constructor does; retain the native rule that a dominant default case is not marked for peeling. | Corrected in the edge-reconstruction batch. Six switch cases fail against the old constructor; coverage includes a dominant default, the same final case without a default, duplicate destinations and sample/fraction thresholds. |

| B071 | Port prerequisite / EH-aware predecessors | Natural-loop discovery needs `BlockPredsWithEH`, including filter-to-enclosed-finally/fault flow during second-pass EH; these predecessor helpers were absent. | Port handler-start lookup, nullable cached predecessor heads and SSA stress hashing/shuffling. Reuse the already implemented DFS second-pass successor logic through the native visitor contract rather than duplicating it. | Ported with 21 Debug / 10 Release focused cases: regular-list identity, noncontiguous try blocks, handler/filter starts, callfinally-tail exclusion, empty-cache invalidation, two-pass region order/abort, unsigned stress seeds and link permutation. Existing 24 DFS cases also pass in both configurations. Natural-loop discovery follows in B072; full synthesis remains unimplemented. |
| B072 | Port prerequisite / natural-loop discovery | Profile synthesis needs natural-loop membership, nesting, edge classification and traversal; the collection was a stub and descriptors were absent. | Port complete discovery, canonicalizability checks, descriptor queries, bit/loop visitors and dumps from the pinned oracle. Retain irreducible-header accounting, EH flow and Wasm branches. | Ported under D002 with 16 Debug / 14 Release focused cases, plus all 45 Debug / 34 Release EH/DFS prerequisite cases. Coverage includes 130-block loops, nested/sibling order, irreducible cycles, EH membership, callfinally rejection, duplicate exits, callback mutation/abort and exact dump text. Wasm execution and native phase parity remain unestablished. Loop optimization/duplication/induction analysis remains in the residual tree. |
| B073 | Bug / full bit-vector construction | `BitSetOps.MakeFullArrayBits` fills with `nint.MaxValue`, omitting each word's high bit, and computes final-word width from array length rather than trait bit count. Native `bitsetasshortlong.h` uses all-one words and `GetSize(env)`. | Correct all-one initialization and final-word masking with boundary coverage when porting the next consumer of full vectors. | Fixed for assertion-table initialization. Twelve focused cases fail with the original implementation and pass with all-one words, size-based final-word masking, and empty-set handling. |
| B074 | Port prerequisite / profile validation and throw paths | Synthesis needs full profile consistency checks, relative weight comparison and EH-boundary classification. Postorder throw-path likelihood adjustment was a no-op. | Port checker options, phase/config selection, incoming/outgoing flow checks, deferred failures and exact diagnostics. Propagate guaranteed throws once in native postorder; modify only heuristic conditional edges and invalidate consistency only when using profile weights. | Ported with 31 Debug / 17 Release focused cases and full suites of 837 Debug / 708 Release cases. Covers missing likelihoods, relative comparisons, exceptional/OSR inputs, unprofiled/deferred blocks, config selection, exact successful dumps and conditional/switch throw propagation. Native quirks remain: likelihood-sum checking alone does not enable the aggregate checker, and missing profile flags assert independently of its Boolean result. Synthesis's main algorithm and profile incorporation remain unimplemented. |
| B075 | Upstream bug / double configuration parsing | Native `ConfigDoubleArray::Init` advances solely to `strtod`'s end pointer, with no non-progress check or `errno` reset. Non-numeric input or trailing whitespace can loop indefinitely; its null-input comment also conflicts with unconditional dereferencing. The synthesis caller indexes the first element even for an empty string. | Use managed .NET 11 partial/hexadecimal parsing as directed by the user; document grammar, range and failure differences under R003. | Managed UTF-8 parsing removes CRT/errno and Windows dependencies. Twenty-eight Debug cases cover conversion, range boundaries, culture, failure and layout. Trailing separators are accepted; malformed input and empty required settings fail explicitly. Hex floats require an exponent, overflow/underflow follow managed semantics, and CRT NaN payload spellings are unsupported. No blanket dump-normalization exception is claimed. |
| B076 | Port prerequisite / likelihood policies and cyclic gains | Synthesis requires ordered heuristic assignment, repair/blending and nested-loop gains before solving block weights. | Port the constructor, jump/conditional/switch policies, summation, clear/reverse/random modes and complete cyclic-probability computation, including capping and first-eligible-exit repair. Retain Debug-only stress modes and the distinction between initial heuristic markers and repair/blend updates. | Ported with 26 new cases in each configuration; combined synthesis/loop selections pass 73 Debug / 57 Release cases. Covers heuristic priority, shared conditional edges, duplicated switch cases, zero/inconsistent profiles, seeded draws, nested gains, cap threshold, both exit orientations, first-exit ordering and infinite loops. Solver and driver support follow in B077/B078. No full profile-phase activation or native parity claim. |
| B077 | Port prerequisite / profile block-weight solver | Synthesis must handle natural and irreducible cycles, EH input counts and bounded convergence before its main driver can run. | Port direct block weighting, solver selection and complete Gauss-Seidel solving. Preserve DFS order, predecessor summation, self-edge gains, fifty-iteration bound, residual/overflow checks and pre/post-import policy. | Ported with 15 new cases per configuration; combined synthesis/loop selections pass 88 Debug / 72 Release cases. Analytical iteration tests assert their DFS order explicitly. Covers diamonds, natural/irreducible loops, unrecognized self-edges, overflow, unreachable blocks, entry/exit imbalance and implicit/explicit EH flow accounting. Native single-pass overflow and convergence ordering is retained. Input-weight assignment and main `Run` follow in B078. |
| B078 | Port prerequisite / synthesis orchestration and input weights | Profile incorporation requires a complete synthesis driver rather than disconnected weight/likelihood helpers. | Port all seven policy options, input normalization/reset, reachable EH seeding, bounded repair retries and factor modulation, source/consistency flags, single-edge heuristics, metrics, called count and deferred Debug checking. | Ported with 44 new Debug / 33 Release driver/input cases. Combined synthesis/loop/parser selections pass 156 Debug / 105 Release; full suites pass 946 Debug / 782 Release, zero skipped. Tests cover loop-entry normalization, inlinee EH exclusion, all options and source policies, retry bounds, single-edge exclusions, called counts, configuration overrides and invalid pre-import flow. Phase integration and NativeAOT capture follow in B079/B080; R003 records parser policy separately. |
| B079 | Port phase / profile incorporation | The profile phase remained a no-op despite implemented lookup, reconstruction and synthesis support. | Port complete stress/minopts/configuration selection, schema summary, edge-over-block precedence, repair/reset, blending and inlinee scaling. | Implemented with 23 new Debug / 13 Release cases; full suites pass 969 Debug / 795 Release. NativeAOT recapture reaches all seven methods. Import prefixes for Add, Branch and Locals match exactly; remaining first differences concern inline-candidate processing. This is not full-phase/codegen parity. |
| B080 | Port phase / DFS preparation | `fgDfsBlocksAndRemove` was still a no-op, leaving the tree null when the now-implemented throw-likelihood adjustment ran. | Wire the complete native phase: invalidate annotations, compute and publish DFS, remove unreachable blocks and return the native phase status. | Implemented using existing ported helpers. Existing DFS cases now cover phase publication, invalidation and removal. The native-host corpus completes all seven selected compilations instead of asserting at throw-likelihood adjustment. |
| B081 | Port prerequisite / special-intrinsic call folding | `gtFoldExprCall` was a no-op after late devirtualization, preventing Enum and Type intrinsic optimization. | Port optimization and pre-argument-morph gates, Enum.HasFlag dispatch, exact integral Enum.Equals unboxing/comparison and Type equality/inequality folding. Consume returned replacements rather than changing CLR node classes. | Implemented using existing helpers. The importer already consumes and pushes the returned replacement. Eighteen new cases cover gates, user versus hidden arguments, Type comparisons, Enum underlying types, exactness/mismatches, fault flags and unsupported operands; 15 fail against the original stub. The combined folding/importer/comparison selection passes 90 cases in both Debug and Release. Scalar arithmetic and its native dump evidence follow in B082. |
| B082 | Port prerequisite / scalar constant folding | Scalar operations were not folded because the folding dispatcher and constant helpers were stubs. | Port unary folding, integral/floating constants and casts, binary arithmetic/comparisons, exceptional-operation guards and tree-form constant replacement. Preserve native diagnostics, flags and logical IDs. | Implemented with 47 scalar cases; the initial 44-case selection had 39 failures against the original stub. Eight Debug formatting cases cover native significant trailing zeroes. Full suites pass 1,046 Debug / 860 Release. NativeAOT captures all eight corpus methods; Add, Branch, Locals and FoldConstants have exactly equal import prefixes. FoldConstants includes six folds with matching logical IDs. Thirteen complete native helpers/declarations are retired; partial dispatcher, overflow/VN helpers and shared native bashing templates remain. This is not full-phase/codegen parity. |
| B083 | Port prerequisite / value-numbered folding and ranges | `ValueNumStore` initially lacked constant interning and `IsVNNeverNegative`; the compiler lacked `fgValueNumberTreeConst` and overflow exception-set construction. | Port VN support before permitting value-numbered folding or range queries that require VN facts. Preserve the native value-number and exception-set contracts. | Scalar interning and conservative VN non-negativity are implemented (B090/B091). Tree constant numbering, metadata registration and VN-backed overflow construction are now complete (B100). Already-established tree facts retain their native early returns. Full VN-phase activation and post-morph alias/LIR repair are not claimed. |
| B084 | Port prerequisite / floating constant-operand folding | Floating binary operations with exactly one constant operand were left unchanged. | Port the complete `gtFoldExprSpecialFloating`, including signed-zero identities, NaN arithmetic/comparisons, side-effect wrapping, full-optimization gating and the JTRUE relop restriction. | Implemented and retired from the native residual tree. Forty new folding cases cover operand direction, single/double types, signed zero, unordered comparisons, retained effects and optimization gates; 28 initial regressions fail before implementation. The combined selection passes 130 Debug / 87 Release cases, including diagnostic coverage. Five of nine corpus import prefixes match exactly; FoldFloating includes all four identity folds. The broader binary dispatcher remains partial, and no full-phase/codegen parity is claimed. |
| B085 | Port correctness / numeric dump parity | Signed-minimum integer constants threw during checked magnitude negation, and importer floating literals omitted the native `%#.17g` significant zeroes. Both surfaced in the signed-zero folding corpus. | Preserve native unsigned magnitude conversion and reuse the shared general floating formatter for both `ldc.r4` and `ldc.r8` diagnostics. | Corrected with four integer dump cases, two added floating formatter cases and the exact FoldFloating import prefix. No dump normalization or behavioral deviation was introduced. |
| B086 | Port prerequisite / integral ranges | Assertion subranges and morph cast reasoning need the missing `IntegralRange` model; `GenTree.IsNeverNegative` omitted its range query. | Port symbolic bounds, containment/union, type and tree inference, cast input/output contracts and diagnostics. Preserve signed-domain interpretation, local normalization, span/array bounds and intrinsic rules. | Complete range types and nine native definitions retired. Fifty-four Debug / 53 Release cases cover range endpoints, 32 checked-cast combinations against mathematical overflow, unchecked representation rules, locals, calls, conditional/masked trees, span/array lengths and intrinsics. Three cases fail with the old non-negativity stub. Full suites pass 1,146 Debug / 953 Release; NativeAOT retains the five exact import prefixes. The corpus is not dedicated range/codegen evidence. Assertion descriptors, tables, generation and morph completion remain unported; VN-dependent queries are explicit NYI (B083). |

| B087 | Port prerequisite / assertion representation and tables | Local assertion propagation needs descriptors, owned constants, table/dependency storage, complementary lookup and diagnostics. | Preserve native copy/equality semantics, null-VN hints, sizing, one-based indices and dump spelling. Keep VN expression dependencies outside this bounded support batch. | Implemented representation, non-VN-query factories, initialization, dependency expansion, lookup/reset, complementary mapping/search, VN membership and diagnostics. Sixteen complete native definitions are retired; the mixed-completion descriptor declaration remains because its VN-dependent factories are unported. Focused assertion/bitset selection passes 28 Debug / 27 Release cases, including five executed fixed-vector payload cases. Insertion, complement creation, generation and morph completion remain unported. No phase activation or new parity claim. |
| B088 | Suspected upstream bug / assertion reset | Native insertion indexes every `O2K_LCLVAR_COPY` on both operands, but `optAssertionReset` removes the second dependency only when `IsCopyAssertion` is true, which also requires equality. An inequality-copy entry could leave a stale second-operand dependency after reset. | Verify whether a within-block local assertion path can produce such an entry before changing behavior or reporting upstream. | Recorded from the native insertion/reset contracts; execution reachability is not established. The port preserves native reset behavior. |
| B089 | Port prerequisite / local assertion invalidation and queries | Morph completion needs invalidation of parent/field dependencies in both active sets; local propagation needs ordered subrange/constant queries and access-width checks. | Port the complete helpers without activating unfinished generation or expanding into the VN store. | Six complete native functions retired. Existing assertion coverage extended by five focused cases for parent/field/copy invalidation, postorder-only activity, live-set filtering, first-match order and small-field access width. Combined selection passes 33 Debug / 32 Release cases. The native diagnostic tree parameter is retained in both managed build configurations but used only in Debug. Insertion, generation and morph completion remain pending. |

| B090 | Port prerequisite / VN-backed assertion insertion | Global assertion insertion needs scalar VN storage, stable function views and underlying-addend recognition; expression VNs require innermost-loop provenance. | Preserve native chunk numbering, constant bit identity, function keys, duplicate order and dependency registration. | Implemented scalar/handle interning, reserved VNs, nullary/non-folding binary applications, function queries, block-to-loop mapping, complete local/global insertion and complement creation, and three VN-backed descriptor factories. Retired 35 complete native definitions and two representation types; mixed-completion storage/templates remain. The combined selection passes 107 Debug / 103 Release cases, zero skipped. General VN evaluation, phi traversal, non-negativity, assertion generation and morph completion remain pending. No new phase/codegen parity claim. |

| B091 | Port prerequisite / VN non-negativity | The remaining assertion factories and tree non-negativity fallback require complete VN reasoning, including reaching definitions and HW intrinsic metadata. | Port phi allocation/query/traversal, scalar extraction, array-length/log2 recognition and all native non-negativity cases; wire the two factories and conservative tree query. | Implemented and retired 15 complete native definitions plus phi/visitor representations. Eight focused cases cover cyclic/nested phi traversal, copied inputs, conservative rather than liberal facts, mask element-count thresholds and 32/64-bit log2 patterns. Combined selection passes 87 Debug / 85 Release cases, zero skipped. General VN evaluation, memory-phi operations, assertion generation and morph completion remain pending. No phase/codegen parity claim. |
| B092 | Managed representation correctness / SSA allocation | Default `SsaDefArray<T>` had null storage, but its first allocation dereferenced the array length. Actual phi setup exposed the missing empty-state handling. | Allocate from a null/empty backing array and retain existing growth, indexing and reset semantics. | Fixed; the cyclic-phi case exercises initial allocation, growth, definition lookup and reset/reuse. No native algorithm change. |

| B093 | Port prerequisite / bounds assertion generation | Conditional bounds assertions require checked-bound/index tracking and complete VN comparison recognition. | Port `optCreateJTrueBoundsAssertion` with unsigned edge polarity, cast handling, constant normalization, equality exclusions and table-pressure gates intact. | Implemented with ten VN helpers and the conservative-normal-VN adapter; twelve complete native definitions and the unsigned-bound result type retired. Six focused cases cover four unsigned edge forms, ulong casts, constant-minus-one normalization, signed-minimum subtraction rejection, equality exclusions and registered-fact gating. Combined selection passes 93 Debug / 91 Release cases, zero skipped. General JTRUE/node assertion generation and morph completion remain pending; this helper is not yet reached through a newly activated phase. |

| B094 | Port prerequisite / general assertion creation and JTRUE facts | General branch facts need scalar/vector assertion creation, copy/normalization gates and exact/subtype recognition beyond bounds comparisons. | Port complete local/global creation and JTRUE generation, including comma-wrapped CSE values and edge polarity. | Four native definitions retired. Eight focused cases cover negative-offset null facts, small-local stores versus comparisons, copy normalization/redefinition, integral-only SIMD equality and comma-wrapped exact-type edges. Combined selection passes 101 Debug / 99 Release cases, zero skipped. Node-wide `optAssertionGen` and morph completion remain pending. |
| B095 | Translation defect / null-check offset signedness | `fgIsBigOffset` compared signed offsets although native accepts `size_t`; negative offsets could incorrectly establish non-null assertions. | Preserve native unsigned comparison without changing signed-offset callers. | Corrected with unchecked native-width conversion. Negative, zero and positive offsets are exercised through both the predicate and assertion creation. |

| B096 | Deferred translation defect / mask all-bits-set | `simdmask_t.AllBitsSet(elementCount)` ignores its argument and writes `uint.MaxValue`; native uses `GetBitMask(elementCount)`. | Correct the mask constructor and its consumers when that path is ported; cover counts below and above 32. | Observed while inspecting fixed-mask VN storage. Constant interning/retrieval does not use this constructor; left unchanged in that batch. |
| B097 | Port prerequisite / fixed-width VN constants | Node-wide assertions require zero-by-type and widening-cast normalization; generic constant import also needs fixed vector/mask storage. | Preserve bit identity, copied payload ownership, type chunk grouping and unsigned widening gates. | Eighteen complete Windows-x64 native definitions retired. Seven focused cases cover all five vector widths, zero padding, numeric signedness, handles, fixed masks and cast normalization. Combined selection passes 108 Debug / 106 Release cases, zero skipped. ARM64 scalable/mask storage remains explicitly NYI; mixed native constant templates remain. General unary VN expressions and node-wide assertion generation are still pending. |

| B098 | Port prerequisite / unary VN expressions | Node-wide assertion generation needs unary array-length VNs with actual native folding, not only function interning. | Complete scalar unary folding, known allocation/frozen-object/readonly-field lengths, field-sequence identity and required exception composition. | Eighteen complete native definitions retired. Combined selection passes 136 Debug / 133 Release cases, zero skipped, including integer wraparound, signed zero and NaN payloads, stable views, EE flags, canonical identities and ordered exception unions. Field-sequence tokens are managed metadata under D002. General multi-argument VN evaluation and node-wide assertion generation remain pending; no phase/codegen parity claim. |

| B099 | Port prerequisite / node assertions and morph completion | Morph completion needs node-wide generation, edge-specific live sets, implied boolean ranges and physical-definition invalidation. | Complete `optAssertionGen`, `fgAssertionGen` and both `fgMorphTreeDone` entry points before wiring remaining folding/morph consumers. | Four complete native definitions retired. Sixteen focused cases cover kill-before-gen ordering, conditional suppression/edge separation, implied ranges, global/early-propagation gates, conservative VN facts, array helpers and tail-call null checks. Combined selection passes 152 Debug / 149 Release cases, zero skipped. No new phase is activated; general local propagation and block/global morph remain unported. |

| B100 | Port prerequisite / VN-backed constant folding | Constant replacement and overflow folding stopped with explicit NYIs when a VN store was present. | Complete all Windows-x64 tree constants, handle/field metadata registration and paired overflow exception construction without changing native morph gates. | Eleven complete native definitions retired. Combined selection passes 197 Debug / 191 Release cases, zero skipped. Eight new cases plus extended vector/mask cases cover refreshed pairs, reference/byref zero, bit preservation, unknown compile-time handles, field registration and overflow exceptions. ARM64 scalable/mask storage remains explicitly NYI. No new phase or codegen parity claim. |
| B101 | Port prerequisite / non-HWI expression folding | Binary dispatch omitted integer identities, identical comparisons, conditional selection and nullable-box comparisons. | Complete binary/special/compare/conditional/nullable-box folding and nested conditional-flag cleanup, retaining native optimization gates, effects, morph finalization and node identities. | Five complete functions and one visitor retired. Fifty-seven new cases; 45 fail against the previous dispatcher. Combined selection passes 254 Debug / 248 Release, zero skipped. Refreshed NativeAOT/native capture has six of ten exact import prefixes, including the new FoldInteger mask-to-cast fold; the four inline-related differences remain. HWI folding, full inlining/global morph and codegen parity remain pending. |
| B102 | Port prerequisite / vector creation constants | HWI folding needs recognition and lane assembly for Create/CreateScalar/CreateScalarUnsafe. | Preserve native broadcasts, zero upper lanes for CreateScalar, integer truncation, floating bits and output mutation when an argument is not constant. | Both complete helper definitions retired. Eighteen new cases; the combined HWI/scalar selection passes 192 Debug / 188 Release, zero skipped. Managed output uses the existing maximum-width SIMD value. The 32-bit decomposed-long branch is source-ported but unexercised. No dispatcher/phase activation or new corpus/codegen claim. |
| B103 | Port prerequisite / HWI operation classification | HWI folding lacks intrinsic-to-operator mapping and effective NEG/NOT recognition. | Preserve native target guards, scalar flags, multiply type gates, bitwise classification and lane identity queries. | Eight complete definitions retired. The mapping retains 146 target-guarded cases. Forty-four new cases; the combined HWI/scalar selection passes 236 Debug / 232 Release, zero skipped. Floating zero queries compare bits, so negative zero is not positive zero; floating zero-subtraction is not reclassified as NEG. ARM64/Wasm branches are source-ported, not executed. No HWI dispatcher or new codegen/parity claim. |
| B104 | Port prerequisite / vector unary evaluation | HWI folding lacks scalar and fixed-width vector NEG/NOT/LZCNT evaluation. | Preserve wrapping integer arithmetic, lane widths, bitwise floating dispatch, xarch scalar upper lanes and inactive node storage. | Eight complete Windows-x64 definitions retired. Twenty-six new cases; the combined HWI/scalar selection passes 262 Debug / 258 Release, zero skipped. Coverage includes signed minima, unsigned negation, zero counts, signaling-NaN bits, signed zero, all fixed widths and inactive padding. ARM64 scalable evaluation remains explicitly NYI; fixed-width ARM64 scalar clearing is source-ported only. No dispatcher or new corpus/codegen claim. |
| B105 | Port prerequisite / vector binary evaluation | HWI folding lacks scalar and fixed-width binary constant evaluation. | Preserve wrapping arithmetic, signed/unsigned comparison masks, zeroing/sign-propagating SIMD overshifts, masked rotates and raw floating bitwise inputs. | Thirteen complete native definitions retired. Forty-seven new cases; combined HWI/scalar selection passes 309 Debug / 305 Release, zero skipped. Coverage includes narrow signed division, negative/oversized shift counts, floating masks/NaN/signed zero, scalar upper lanes, explicit widths, aliased inputs and inactive storage. ARM64 scalar clearing and other-target shift masking remain source-ported only. No HWI dispatcher activation or new corpus/codegen claim. |
| B106 | Port correctness / mask evaluation prerequisites | `simdmask_t.AllBitsSet` ignored its element count and always set only 32 bits. Mask evaluation and vector conversion helpers were absent. | Use the existing bit-mask helper, retain the native xarch eight-bit minimum and all-true normalization, and distinguish vector sign-bit extraction from nonzero-lane tests. | Five mask-storage cases fail before correction. Twenty-eight new cases pass; combined HWI/scalar selection passes 337 Debug / 333 Release, zero skipped. Ten complete Windows-x64 native definitions retired. Coverage includes inactive bits, ignored scalar flags, aliased mask operands, all lane widths, bit ordering, noncanonical floating data and conversion results that must not normalize unused mask bits. ARM64 predicate spacing/nonzero tests remain source-ported only; scalable evaluation and HWI activation remain pending. |
| B107 | Port prerequisite / HWI mask conversion folding | HWI folding lacks conversion recognition, vector-to-mask construction, constant conversion folding and sequential MSB extraction. | Share sign-bit extraction with xarch vector conversion, preserve mask-use bookkeeping and fresh-node identity, and leave morph/VN finalization to the folding caller. | Eight complete Windows-x64 definitions retired. Eight new cases plus extended extraction checks; combined HWI/scalar selection passes 345 Debug / 341 Release, zero skipped. Inputs and conversion operands remain unchanged. Base-node properties serve both generic and HWI nodes, avoiding duplicate native query methods. ARM64 predicate construction is source-ported only; scalable conversion folding is explicitly NYI. Full HWI dispatcher remains pending. |
| B108 | Port prerequisite / per-element vector masks | HWI folding needs proof that each vector lane is entirely zero or one before simplifying mask-related expressions. | Recognize raw constant lanes, existing local annotations, intrinsic return-mask metadata and recursive bitwise expressions. Preserve larger-to-smaller lane compatibility and reject SIMD-size mismatches. | Four complete definitions retired. Twenty new cases; combined HWI/scalar selection passes 365 Debug / 361 Release, zero skipped. Coverage includes all arithmetic lane types, mixed/noncanonical bits, inactive padding, unsigned/signed/float width equivalence, local facts and recursive intrinsics. ARM64 comparison fallback is source-ported only. Value-number mask recognition and full HWI folding remain pending. |
| B109 | Port prerequisite / HWI reconfiguration | HWI folding changes intrinsic IDs and operand counts within the same managed node class. | Preserve existing node metadata and flags, use existing ID validation/normalization, and replace managed operand storage only for full resets. | Five complete native definitions retired. Six new cases; combined HWI/scalar selection passes 371 Debug / 367 Release, zero skipped. Covers ID-only/partial changes, operand growth/shrinkage, empty operands and caller-owned flags. Managed reset needs no compiler allocator; borrowed operand views become obsolete. This does not permit cross-kind node bashing or activate HWI folding. |
| B110 | Port prerequisite / comparison and shift metadata | Full HWI folding needs float comparison-mode normalization and recognition of per-lane variable shifts. | Preserve all 24 mapped mode labels across vector/scalar/mask forms and retain original IDs for the other eight modes. Reuse the framework comparison enum after checking all encodings. | Two complete functions and one enum retired. All 32 native names/values and the byte underlying type match the framework enum. Twenty-six new cases; combined HWI/scalar selection passes 397 Debug / 393 Release, zero skipped. Covers every signaling pair, unchanged modes, vector widths, scalar/mask forms and all six variable-shift IDs. Full HWI dispatcher remains pending. |
| B111 | Port completion / Windows-x64 HWI folding | `gtFoldExpr` omitted the HWI dispatcher; mask-zero construction was missing and Debug destruction poisoned flags before use-edge traversal. | Complete creation, unary/binary/ternary folding and conversion rewrites; preserve scalar lanes, signed zero/NaN restrictions, side effects, node identity, morph marking and VN refresh. Traverse uses before poisoning and construct mask zeros through the existing factory. | One complete Windows-x64 dispatcher definition retired; other targets remain deferred to the oracle. Sixty-five new Debug / 61 Release cases; combined selection passes 462 Debug / 454 Release, zero skipped. Forty-one cases fail against the old dispatcher; six conversion cases exposed the Debug destruction defect, and a false-mask comparison exposed the missing zero case. NativeAOT capture retains six exact import prefixes across eleven methods. The new FoldHardware vector probe diverges at the existing impHWIntrinsic stub, so vector import/execution parity is not established; the four inline-related differences remain. Full inlining/global morph and codegen remain pending. |
| B112 | Activation gate / inline phase prerequisites | Completing expression folding does not make the inline phase ready. `compCompile` invokes stubbed early qmark expansion, patchpoint/indirect-call transforms and post-import cleanup before inlinees return, then a stubbed morph initializer before root inlining. | Keep `fgInline` disabled. Complete the prerequisite functions and validate successful nested compilation before activating it; do not substitute no-op phases for the native pipeline. | Audit traced the driver, placeholder walker, call-morph path and native phase bodies. `fgMorphInit` additionally needs missing `fgInitThisClass` and `hashBv.Create`; `hashBv.Init` is also still a stub. Next bounded batch: root class-initialization construction, reusing the existing shared-cctor, runtime-context and ReadyToRun helpers. The separate `impHWIntrinsic` import gap remains explicit in B111. |
| B113 | Port completion / root class initialization | `fgInitThisClass` was absent, preventing complete morph initialization. | Preserve direct/shared-cctor lookup, all three CoreCLR runtime-context forms and NativeAOT shared-token/nonshared-class helper selection. Reuse existing factories and retain generic-context reporting and argument order. | Complete native definition/declaration retired. Thirteen controlled-EE cases added; the existing fixture passes 29 Debug / 29 Release, zero skipped. CoreCLR ReadyToRun stays on the CoreCLR path; NativeAOT uses the zeroed token and context only for shared classes. Existing nullable ReadyToRun helper rejection propagates. No phase activated and no unchanged-frontier corpus recapture. `hashBv` and the remaining B112 gates are still pending. |
| B114 | Port completion / morph initialization and temporary storage | `fgMorphInit` was a no-op and `hashBv` had no storage. | Complete E&C frame requirements, root cctor insertion, Debug GC checks and stack-check locals. Port hash-vector creation, lookup, insertion, removal, resizing and callback traversal without changing bucket/node/bit order. | One phase and 27 hash-vector definitions retired at the initial storage boundary. Its focused selection passed 47 Debug / 44 Release, and the then-eleven-method corpus finished morph initialization. Subsequent set-algebra support adds native AND/OR/subtraction, comparison, independent copies and compound operations; the storage/algebra selection passes 40 Debug / 40 Release. Vector XOR/Intersects (B246/B247), countBits/setLowest (B115) and iterator APIs remain deferred. These primitive results do not establish optimization-phase parity. |
| B115 | Suspected upstream defects / deferred hash-vector arithmetic | Pinned `hashBvNode::countBits` adds both `PopCount(bits)` and the raw element value. `setLowest` fills physical 64-bit elements on AMD64 while the logical element stride is 32 bits, potentially exposing overlapping indices through traversal. | Confirm intended semantics and upstream status before porting these APIs; do not silently fix native behavior only in C#. | Recorded during B114. Neither API is needed by the ported single-bit storage path; their native bodies remain in the residual tree. |
| B116 | Port completion / qmark expansion | Early/late `fgExpandQmarkNodes` was a no-op; qmark shape checks, expansion and throwing-block conversion were absent. | Preserve traversal of newly inserted blocks, condition reversal, both/single-arm shapes, local/field writeback, comma splitting, no-return conversion, EH extension, flags, likelihoods and phase status. | Seven complete native definitions retired. Seventeen focused cases added; qmark/block-splitting selection passes 60 Debug / 60 Release, zero skipped. Tests include nested early/late gating, EH boundaries and callfinally-tail preservation. NativeAOT capture finishes the early phase unchanged for all eleven corpus methods and retains six exact import prefixes. Actual qmark CFG transformations are covered by constructed IR cases, not native execution parity. Patchpoint/indirect-call transformations and post-import cleanup still gate inliner activation. |
| B117 | Suspected upstream defect / single-arm qmark likelihoods | In the true-only expansion, native reverses the condition but assigns `ThenNodeLikelihood` to the edge skipping that arm, while giving the arm block that same percentage of incoming weight. Non-50/50 inputs can therefore disagree between edge and block weights. | Preserve native assignments during the clean port; confirm and address upstream behavior separately before changing compiler decisions. | B116 retains this behavior and explicitly checks an 80/20 true-only case. No managed-only correction applied. |
| B118 | Port completion / patchpoint transformation | `fgTransformPatchpoints` was a no-op despite imported regular/partial-compilation patchpoint flags and generated IR operators. | Complete shared-counter allocation/entry initialization, regular decrement/test/helper CFG expansion and forced partial-compilation replacement. Preserve 99/1 probabilities, weights, flags, IL offsets and method gating. | Entire `patchpoint.cpp` retired (transformer and phase). Seven focused cases added; patchpoint/block-splitting selection passes 50 Debug / 50 Release, zero skipped. NativeAOT capture finishes the phase unchanged for all eleven non-Tier0 corpus methods, retaining six exact import prefixes. Constructed IR establishes transformation behavior; patchpoint metadata/codegen and OSR execution remain unavailable. Indirect-call transformation and post-import cleanup still gate inliner activation. |
| B119 | Port prerequisite / indirect-call transformation | Fat-pointer calls and guarded devirtualization require shared spilling and CFG expansion before the indirect-call phase can be enabled. | Port complete specializations and prerequisites, preserving argument evaluation, return placeholders, multi-guess/exact checks and chained GDV; enable the phase only after both paths are complete. | Shared `Transformer`, `FatPointerCallTransformer` and `gtCloneStmt` complete; 23 native definitions retired. Twelve focused cases cover void/value calls, EH boundaries, tuple loads, hidden-argument placement, ordered single evaluation, reference-class preservation and inferred local types. Related selection passes 90 Debug / 90 Release, zero skipped. Guarded devirtualization, candidate-call cloning, virtual-vtable target expansion and phase activation remain. No new corpus or native execution-parity claim. |
| B120 | Translation defect / inferred local load types | `gtNewLclVarNode(TYP_UNDEF, ...)` inverted native normalization: ordinary small locals stayed small while loads requiring normalization became actual types. The indirect-call spill helper needs this factory. | Preserve the precise type when `lvNormalizeOnLoad` is true; otherwise select `ActualType`. Explicit requested types remain unchanged. | Corrected in B119. Four byte/ushort parameter/local cases fail before the fix and pass afterward. |
| B121 | Suspected upstream defect / fat-pointer profile likelihoods | The shared indirect-call transformer assigns 80/20 thin/fat block weights but sets both outgoing check edges to 0.5; native has an explicit likelihood TODO. | Preserve native assignments in the clean port; investigate and change upstream separately before altering profile decisions. | B119 retains and tests both the block weights and equal edge likelihoods. |
| B122 | Port prerequisite / GDV candidate cloning and virtual targets | Guarded devirtualization needs safe cloning of calls with return placeholders and construction of vtable target lookups. | Reuse the existing call-clone implementation without changing ordinary clones; preserve candidate-info sharing until caller repair. Port unchunked, chunked and relative vtable lookups, including global-morph exception flags and native unsigned offset arithmetic. | `gtCloneCandidateCall` and `fgExpandVirtualVtableCallTarget` complete; both native definitions/declarations retired. Existing local `gtCloneCall` extracted unchanged as a private shared member. Twelve additional cases cover inline/mono/poly-GDV metadata, return-placeholder ownership, no-return tracking, deep argument clones, register state, all lookup shapes and offset wraparound. Combined selection passes 102 Debug / 102 Release, zero skipped. GDV expansion and phase activation remain. |
| B123 | Port completion / guarded devirtualization and indirect-call phase | Complete the B119/B122 pass with class/method/delegate guards, return repair, conditional profile mass, exact guesses and chained hot paths. | Preserve native lookup shapes, single-definition invalidation, small returns, candidate and enumerator-map ownership, cloning barriers, cold-path bypass and diagnostic text. Supply owning-link lookup and statement unlinking; activate only the complete pass. | Remaining `indirectcalltransformer.cpp`, `gtFindLink`/its walker and `fgUnlinkStmt` retired (33 definitions). Thirty-six additional cases; complete related selection passes 138 Debug / 138 Release, zero skipped. NativeAOT/corpus succeeds with six exact import prefixes and eleven exact outer-method no-candidate phase sections. The corpus does not exercise fat-pointer/GDV expansion; constructed IR/controlled EE cases cover those transformations. Post-import cleanup still gates inliner activation; codegen parity remains unavailable. |
| B124 | Suspected upstream defect / multi-guess GDV metrics | Native increments `MultiGuessGDV` only inside the branch requiring `GetChecksCount() == 1`, then tests `GetChecksCount() > 1`. The increment is unreachable; multi-guess and exact calls also bypass that branch's `GDV` increment. | Preserve native metrics during the clean port; confirm the intended counting policy upstream before changing dump/statistical output. | B123 preserves the native guards and increments. |
| B125 | Translation defect / flag-filtered node searches | `FindNodeVisitor` accepts `requiredFlagsToDescendIntoTree` but does not assign `_requiredFlagsToDescendIntoTree`, so the visitor tests the default zero mask. `gtFindNodeInTree` still checks the root, but descendant pruning is lost. | Restore constructor assignment in a separate bounded fix and check root versus descendant pruning, including zero-mask callers. | Corrected after the inliner milestone. Eight cases cover root rejection, matching roots/descendants, all-bits masks, excluded descendants and zero-mask traversal; three fail before the fix. Related importer/tail-call selection passes 65 Debug / 65 Release, zero skipped. No new native definitions retired or corpus capture needed. |
| B126 | Port completion / post-import cleanup | Unimported blocks, EH extents and OSR entry flow were left unchanged by the phase stub. | Complete return-spill refinement, predecessor removal, EH deletion/trimming and normalized OSR step-block construction, including nested/mutually protecting regions and zero-weight profiles. | Complete phase and three EH helpers retired. Twenty-nine focused cases; related selection passes 132 Debug / 132 Release, zero skipped. NativeAOT capture retains six exact import prefixes and eleven exact outer-method no-change post-import sections. Constructed IR covers removal/EH/OSR transformations; the corpus does not exercise these paths or establish codegen parity. B112's named prerequisite phases are now implemented; successful nested-compilation validation and complete `fgInline` activation remain. |
| B127 | Incomplete diagnostics / EH verification | `fgVerifyHandlerTab` remains an empty Debug diagnostic called by existing EH transformations and post-import cleanup. | Port the complete checker and validate its normalization, nesting, filter and funclet rules in a dedicated batch; do not describe the stub as verification. | Existing gap retained explicitly during B126. Cleanup assertions and focused structural checks run, but the full native EH diagnostic does not. |
| B128 | Port prerequisite / inline decision reporting | `InlineResult` began EE inline observations but lacked native scope-exit `Report`, including dump output, permanent rejection propagation and decision callbacks. | Port the whole reporting function and preserve destructor scope ordering through deterministic managed disposal at all production owners. | Report/destructor retired. Eleven new controlled-EE cases cover suppression, repeated reporting, first failure observations, eligible/ineligible NOINLINE propagation, undecided/special-success/VM-failure results and unwinding. Related selection passes 178 Debug / 152 Release, zero skipped. The complete inline phase remains pending; corpus recapture is deferred until that activation. |
| B129 | Incomplete diagnostics / EE text logging | Shared `Globals.vlogf` still returns false under a varargs-support TODO, so `JITLOG` cannot forward native text logs to the EE. | Implement the actual formatting/ABI contract in a separate bounded batch; preserve ordinary dump output and structured EE decision reporting meanwhile. | Identified while wiring B128. Existing dump fallback remains; no EE text-log parity is claimed. |
| B130 | Port prerequisite / boxing patterns | `impBoxPatternMatch` returned unsigned zero from its stub, falsely reporting a successful fold and leaving unboxed values on the stack. Actual nested vector fallback compilation reached an invalid `unbox.any` stack assertion. | Restore the signed byte-count/-1 contract and port all native box/unbox, branch, isinst and nullable patterns, including their inline observations and nullable field helpers. | Three native definitions and two remaining declarations retired. Twenty controlled-EE cases pass in Debug and Release, covering unmatched/truncated input, exact/uncertain/enum-compatible types, branch consumption and isinst results. The complete corpus now imports the previously failing scalar fallback. Nullable field conversions are ported but are not exercised by these scalar cases. Inliner activation evidence is recorded with the enclosing batch. |
| B131 | Translation defect / inline-disable range | An absent `JitNoInlineRange` initialized an empty match-all range; subsequent calls incorrectly disabled inlining. Clearing configured selection to null also left the cached range active. | Apply native's null-configuration guard before consulting the cached range on every call. | Four Debug cases cover absent, matching, nonmatching and empty selection across repeated calls and clearing the setting. Three fail before the fix. |
| B132 | Translation defects / inline compiler lifetime and result | Managed inlinees reused an already-constructed compiler, retaining previous method metadata and SIMD state. Completed inline imports also returned the root's temporary `CORJIT_SKIPPED` result. | Reconstruct each attempt, as native placement-new does, and return `CORJIT_OK` for completed inline IR while retaining root fallback until codegen exists. | Actual native-host capture progresses from failed scalar inlining and a second-attempt SIMD assertion to one successful scalar inline and 43 nested vector-fallback inlines. Fresh managed objects replace native storage reuse; no pooling or partial reset is introduced. |
| B133 | Port completion / active inline phase | `fgInline` was a no-op despite its imported candidates and return placeholders. | Complete candidate traversal, insertion, placeholder substitution, comma cleanup, deterministic decision reporting, diagnostics and metrics. | Four phase cases added; full related selection passes 336 Debug / 281 Release, zero skipped. All eleven corpus methods finish; ten import prefixes and ten post-inline IR sections match native exactly, including successful `InlineCaller`. Nine full inline-phase sections match; scalar inline diagnostics retain B135, while the hardware case still takes managed fallback because hardware-intrinsic import is incomplete. `fgInline` was already absent from the residual ledger; no second retirement is claimed. |
| B134 | Translation defect / lambda phase status | Native `ActionPhase` ignores lambda return values and reports modifications; C# selected its result-returning delegate for early qmark expansion and both head/tail merge sites. This suppressed native phase-end dumps and checks on unchanged graphs. | Use void actions at the three affected call sites while retaining each underlying function's own phase-status contract. | Corrected; fresh native/managed corpus comparison confirms the scalar inlinee's early-qmark dump now matches apart from B135's profile-check text. Head/tail merge itself remains a stub; only its phase-wrapper contract is corrected. |
| B135 | Suspected upstream diagnostic defect / inlinee profile state | Pinned native `fgPgoDeferredInconsistency` lacks an initializer and is assigned by profile synthesis, which inlinees do not run. Two independent native captures skip inlinee checks as though a previous profile check failed; fresh managed inlinees instead run the checks and report no profiled blocks. | Investigate native initialization separately; do not poison managed fields or suppress checks merely to reproduce an apparently uninitialized flag. | Remaining scalar inline-phase text difference; post-inline IR is exact. Native binary embeds the pinned commit and repeated captures agree. This is not an accepted output exception or a claim of full diagnostic parity. |
| B136 | Port completion / internal blocks and method entry/exit | `fgAddInternal` was a stub, including generic exception filters, synchronized method protection and reverse-P/Invoke helpers. | Port complete dependencies and phase ordering, including return merging, this-copy, frame-local and JustMyCode behavior. | Complete and active. All sixteen expanded corpus methods finish. Fifteen phase sections match native exactly; all five new cases have exact post-phase IR. Main retains the preexisting allocation-lowering gap B141; the hardware case retains incomplete intrinsic import. Nine phase/helper definitions and seven declarations retired. |
| B137 | Translation defect / region insertion | `fgNewBBinRegion` asserted that a search start block existed even when an insertion point had already been selected, including ordinary end-of-method insertion. Native jumps directly to the selected insertion point. | Require the non-null start block only when performing the insertion-point search. | Corrected. Seven merged-return cases failed before the correction; all 53 return-merging/block-splitting cases pass in Debug and Release. |
| B138 | Translation defect / initial switch edges | `fgMakeBasicBlocks` creates switch descriptors without successors, but `fgLinkBasicBlocks` tried to populate that empty span. The expanded return corpus crashed on its first switch edge. | Allocate successor capacity for the case count, populate unique edges, then publish the initialized count. | Corrected; duplicate and distinct-case regressions pass, and the switch/return-merging corpus completes with exact phase and post-phase IR. |
| B139 | Translation defect / default source locations | Zero-initialized `DebugInfo` bypassed `ILLocation`'s field initializer, attributing synthetic statements to IL offset zero rather than the native invalid offset. | Complement the stored offset so both explicit construction and `default` preserve native location semantics without additional state. | Corrected; synthetic return assertions and existing suites pass. The five new internal-block corpus cases have exact post-phase IR. |
| B140 | Translation defect / newly exercised diagnostics | Object descriptions decoded the entire uninitialized buffer rather than the EE-written span. Runtime-lookup pointers and EH clause flags used non-native formatting. | Decode only returned bytes and use the existing pointer formatter and numeric flag formatting. | Corrected; synchronization and generic-filter phase text matches native. The generic root still shows the deferred-profile diagnostic difference tracked in B135; its cause was not re-investigated in this batch. |
| B141 | Required port / allocation lowering | `ObjectAllocator.DoPhase` was a no-op, leaving `ALLOCOBJ` where native has an allocation helper. | Activate required heap lowering without silently replacing optional stack allocation. Preserve replacement identity, owners and native call effects. | Heap path active. All seventeen minopts allocation/internal-block phase sections and their emitted IR match native. Analysis, EH/object cloning, stack/heap morphing and use rewriting are implemented. Production phase orchestration and real stack-path execution remain outstanding. Stack-allocation requests report `CORJIT_IMPLLIMITATION`; this is not an accepted output exception. |
| B142 | Diagnostic fidelity / string literal quotes | Optimized Main now reaches a preexisting `CNS_STR` difference: `eePrintStringLiteral` emits `Arg_InvalidOperationException` without native quotation marks. | Reconcile the complete literal-printing contract with native, including truncation and unavailable metadata. | Recorded, outside the required minopts allocation path. This is the first optimized post-allocation IR difference; minopts allocation and internal-block IR match exactly. |
| B143 | Required port / local morph | `fgLocalMorph` was a no-op before implicit-byref retyping and global morph. | Complete local-address traversal, exposure, local transformations and their required bookkeeping. | Complete and active, including optimized assertions, deferred exposure and statement-effect recomputation. Native visitor/support types, phase, exposure helpers and statement-effects walker retired. Local-address store/subtraction corpus cases have exact phase text and emitted IR in both configurations. The broader minopts comparison is 18/19 exact phases; optimized is 15/18. Remaining differences originate in unported hardware import and B147, not accepted output exceptions. |
| B144 | Upstream correctness / volatile promoted fields | Native `LocalAddressVisitor::MorphStructField` permits transforming a volatile access through `FIELD_ADDR` when `GTF_FLD_DEREFERENCED` is set, despite its TODO noting that this is illegal. | Consider an upstream correction separately rather than changing managed memory-ordering behavior during the clean port. | Native guard and transformation preserved, including its load flag-clearing quirk. Focused coverage distinguishes ordinary volatile accesses from the dereferenced-field exception. |
| B145 | Port correctness / SIMD bit conversions | A broader Debug HWI selection encounters overflow exceptions in existing mask conversion/evaluation, integral broadcast/folding and test-side conversions. Examples include casting negative `simdmask_t.RawBits` to `ulong` and narrowing broadcast lane bits. | Reconcile intentional truncation and bit-preserving signed/unsigned conversions with native, including the expectations themselves; do not weaken checked arithmetic globally. | The original broader HWI selection reported 33 failures. Geometric sequence construction subsequently exposed checked narrowing in `GenTreeVecCon.SetElementIntegral`; all six narrowing casts now truncate as the native lane writer does. Six geometric-sequence regressions fail before that correction and pass afterward. The node-reference packet's broader selection still reports the 33 deferred mask/broadcast/folding and test-conversion failures in Debug; its Release selection passes. Exact counts and evidence are in `state.json`. |
| B146 | Translation defect / unsigned local offsets | `IsValidLclAddr` converted its signed offset parameter to `uint` in a checked context, throwing for full-width native offsets instead of rejecting their compact local-address encoding. | Preserve the unsigned reinterpretation and existing range checks. | Corrected with an explicit unchecked conversion. The escaping `uint.MaxValue` regression failed before the fix and now retains pointer arithmetic and the source logical ID. |
| B147 | Required port / early flow-graph update | `fgUpdateFlowGraphPhase` remains a no-op, leaving extra blocks before optimized local morph. Native's early flow-graph update changes Main and ManyReturns; managed does not. | Port the complete update and dependencies when required by the active execution path; do not normalize DFS/block differences away. | Recorded while comparing local-morph inputs. Minopts does not require this optional early update; optimized local-morph phase text retains different DFS ranges for these methods. |
| B148 | Translation defect / arithmetic import effects | `MathOp2Flags` queried the left operand's throwing behavior instead of the newly constructed operation, omitting division exception flags. | Query the operation as native does. | Corrected. The new pointer-subtraction corpus case exposed the missing DIV/RETURN flags in minopts import and local morph; its phase and emitted IR now match exactly. |
| B149 | Translation defect / unsigned checked multiplication | `MulOvf` receives `uns` but drops it when forwarding through `MathOp2Ovf`, which always selects signed arithmetic. | Preserve the unsigned opcode contract and cover unsigned checked multiplication independently. | Recorded during arithmetic-import inspection; not changed as part of local-morph activation. |
| B150 | Required port / implicit-byref parameter retyping | `fgRetypeImplicitByRefArgs` was a no-op before global morph. | Retype pointer-passed structs and transfer or undo promotion with native ownership and profitability decisions. | Complete and active; all twenty minopts phase sections and the new struct-argument case's emitted IR match native. Focused cases cover descriptor-array replacement, retained/dependent/unprofitable promotion, OSR/register annotation clearing and existing post-morph cleanup. Global-morph access expansion is active under B151. |
| B151 | Required port / global morph | `fgMorphBlocks` was a no-op, leaving required tree transformations and their ownership updates inactive. | Port complete driver/tree-morph dependencies with explicit owning replacements; preserve phase order and minopts behavior. | Active, including recursive tree/block/statement morphing, outgoing arguments, tail calls, scalar and hardware transforms, casts, addresses and block copies. All twenty minopts corpus methods complete the phase; nineteen have exact native phase text and resulting IR. `FoldHardware` differs because of the existing hardware-intrinsic import gap. Local assertion application is separated from the still-unported optional global assertion/range phase (D004). Wasm final-argument insertion and ARM64 scalable constants remain explicit deferrals; other-target execution is unvalidated. Later required phases still prevent code generation. |
| B152 | Translation defect / unsigned flag setter | `GenTreeUnOp.IsUnsigned` ignored the assigned value and always set `GTF_UNSIGNED`; assigning `false` did not clear it. | Honor the assigned Boolean while retaining the operator guard and unrelated flags. | Corrected when signed high-multiply dispatch exposed the defect. Both false-assignment cases fail before the fix; all four initial/assigned flag combinations pass afterward. Existing compiler callers assign true and retain their behavior. |
| B153 | Other-target prerequisite / integral constant accessors | Existing `TARGET_32BIT` branches in `GenTreeIntConCommon.IntegralValue` contain a missing semicolon, an untranslated `OperIs` call and an extra closing parenthesis. | Reconcile those accessors before claiming a 32-bit build or executing long-constant replacement there. | Found while adding truncating constant updates; the active Windows-x64 configuration does not compile those branches. Deferred outside required Windows-x64 morphing. |
| B154 | Upstream documentation / arithmetic helper contracts | Native `fgOptimizeMultiply` and `fgOptimizeAddition` promise no state change on a null return, but multiplication's negation motion and addition's initial constant reassociation can update operands and then return null. | Preserve the algorithms; correct the contracts when documenting or restructuring the helpers and do not infer immutability from a null result. | The port retains native in-place operand updates. The commutative driver must continue returning the current tree when no replacement is returned. |
| B155 | Translation defect / Release node value numbers | The fresh `GenTree` constructor initialized `_vnPair` only in Debug, so Release nodes incorrectly started with defined value number zero. Native default-constructs `ValueNumPair` to `NoVN` in all configurations. | Explicitly construct the value-type member in the common constructor, not in individual node factories or transforms. | Corrected after the nonglobal rotation regression failed in Release. Fresh-node coverage checks both numbers and `BothDefined`; related folding, assertion/range and structural-comparison selections pass in both configurations. |
| B156 | Potential upstream defect / XOR rotation recognition | Native `fgRecognizeAndMorphBitwiseRotation` accepts masked complementary variable shifts under XOR. For `(x << (y & 31)) ^ (x >>> ((32 - y) & 31))`, a zero count gives zero, whereas `ROL(x, 0)` gives `x`. | Confirm with an end-to-end native repro before proposing an upstream fix; do not silently change the pinned algorithm only in C#. | Source-level concern recorded during the direct port. Recognition remains identical to the oracle, including XOR roots. Native execution confirmation and any behavior change are deferred. |
| B157 | Translation defect / unary operand replacement | `GenTree.GetUseRefOrNullRef` used binary-only `AsOp()` for both unary and binary nodes. Assertion updates replacing an indirection's address consequently violated the managed node-kind contract. | Use `AsUnOp()` for the shared first operand and access the second operand only in the binary branch. | Corrected with the assertion-application support. The owning-use regression failed in Debug before the fix; statement-root and indirection-address replacements now preserve forward traversal and handle-load flags. |

| B158 | Potential upstream defect / side-effect initialization | Native `SideEffectSet` constructors initialize the effect flags and alias set but not `m_preciseExceptions`, which `AddNode` updates with `|=`. The header has no member initializer. | Confirm the actual native construction/zeroing contract before proposing an upstream correction; do not reproduce uninitialized storage in managed code. | Source-level concern found while translating the side-effect closure. Managed struct fields start at zero; no native execution defect is claimed. |
| B159 | Required port / EH-funclet layout | `SynchronizedReturn` and `GenericCatch` reach rationalization with different native/managed EH block ordering. | Complete the preceding funclet preparation as part of backend EH support, preserving filters, handlers and block order. | Recorded separately from rationalization parity. The scalar minopts execution path does not require these corpus cases to match first; their differences are not normalized away. |
| B160 | Port defect / field-list ownership | `GenTreeFieldList.Uses` returned a mutable struct by value, so `gtCloneExpr` appended fields to discarded copies and returned an empty field list. Lowering's list clearing would likewise leave the owner unchanged. | Fixed by returning the canonical list by reference. | Regression coverage checks cloned field values, independent node/list ownership and clearing the clone without modifying the source. |
| B161 | Port defect / relocation ABI | `ICorJitInfo.recordRelocation` and `getRelocTypeHint` still used `ushort`, while the pinned native interface uses the 32-bit `CorInfoReloc` enum. | Corrected the wrappers, interface and unmanaged vtable signatures together. | Address containment now consumes the typed VM hint, retaining the matched-VM guard and native relocatable-code restrictions. |
| B162 | Port defect / stack argument load size | `GenTreePutArgStk.ArgLoadSize` returned the stored padding delta instead of subtracting it from the stack byte size. | Corrected the getter to decode the native representation. | Three regression cases fail before the fix, including both padded and full-slot loads. Stack lowering widens local loads only within their stack slots and preserves arbitrary-address load widths. |
| B163 | Port defect / floating constant containment | `GenTree.IsCnsNonZeroFltOrDbl` tested for all-zero bits instead of nonzero bits, reversing the native containment predicate. | Negated the bitwise-zero comparison; negative zero remains nonzero for this purpose. | Four regression cases fail before the correction, covering positive zero, negative zero, a finite nonzero value and a NaN payload. Cast and binary containment retain the original constant bits. |
| B164 | Port defect / replacement metadata | Lowering replacements must distinguish full-flag preservation from value-number preservation. Native `SetOper` defaults to `CLEAR_VN`, including when called by `BashToConst`; preserving VNs in these replacements is incorrect. | Keep the constructors' VN clearing for block loads and local-store replacements; explicitly clear VNs in the retyped floating constant. | The contract is defined in native `gentree.h:2104` and `compiler.hpp:2016-2019`. Regressions check clearing for changed operators/constants and retention for unchanged local-field stores and excluded register candidates. |
| B165 | Native source discrepancy / LSRA map copy | The pinned `setInVarToRegMap` copies `regMapCount * sizeof(regNumber)` bytes into a `regNumberSmall[regMapCount]` allocation. On AMD64 these element types are unsigned and unsigned char respectively, so the byte count exceeds the allocation. | The managed port copies `regMapCount` register entries into the existing destination array, preserving split-block aliases. Do not reproduce the out-of-bounds byte copy. | Native `lsra.h:451`, `target.h:164,223,227`, and `lsra.cpp:1978,1994,2139`. Only a declaration and definition were found, with no native callsites; this is a source discrepancy, not an observed runtime failure. |
| B166 | Port defect / intrinsic struct return identity | `isNativePrimitiveStructType` rejected the matching `System.Runtime.InteropServices` namespace instead of rejecting nonmatching namespaces, reversing the classification of intrinsic `CLong`, `CULong` and `NFloat` member returns. | Negate the namespace comparison to match native `compiler.cpp:572`. Reuse the existing `GetReturnTypeForStruct` classifier rather than adding another API. | Five metadata-identity cases fail before the fix. The 29-case classification selection passes afterward, covering sizes, GC layouts, supplied-size query suppression, member conventions and return descriptors. |
| B167 | Port defect / condition-code initialization | `GenTreeCC` ignored the supplied condition, so JCC/SETCC nodes could not retain or reverse the intended condition. | Initialize the owned condition as in native `gentree.h:9498`; use the canonical `gtNewCC` factory from `gentree.cpp:9220`. | Six construction, allocation and reversal cases fail before the fix and pass afterward in Debug and Release. Full comparison lowering and machine-code generation remain separate. |
| B168 | Port defect / SIMD preservation ABI gate | `RyuJitSharp.csproj` enabled `FEATURE_PARTIAL_SIMD_CALLEE_SAVE` for SysV x64 and disabled it for Windows x64, opposite to native `targetamd64.h`. This excluded Windows upper-vector spill/reference bookkeeping. | Enable it for non-Unix x64, retaining the ARM64 setting. Validate spill handling with the feature genuinely enabled rather than treating it as an other-platform path. | Build-property checks match Windows x64, SysV x64 and ARM64. Windows assignment/lifetime coverage now verifies native upper-vector spill weighting with the feature enabled. Allocation remains inactive. |
| B169 | Port defect / switch default removal | `BBswtDesc.RemoveDefaultCase` decremented the case count and then cleared the last entry of the shortened span, destroying a retained case or indexing an empty span for a default-only switch. | Match native count-only truncation, preserving retained case references and the separate successor table. | Corrected with LIR block-splitting support. Both the default-only and multi-case regressions fail before the fix and pass afterward. |
| B170 | Shared port prerequisite / block layout size range | Managed block layouts and value sizes used signed `int`, preventing faithful representation above `Int32.MaxValue`. | Reconcile unsigned layout sizes, keys, value sizes, nonpadding segments and consumers together; do not reinterpret a large block as a negative size or omit initialization. | Shared contracts and init/copy morphing preserve unsigned sizes, including `0x80000000` and `0xfffffff0`. Slot rounding retains native truncation near `UINT_MAX`. No-GC custom layouts omit unused GC arrays under D001. The pending lowering integration now preserves those sizes through actual memset/memzero/memcpy call arguments and full local-heap dispatch; broader call/range dependencies remain uncommitted and lowering is not active. |
| B171 | Port defect / non-GC layout slot queries | `ClassLayout.GetGCPtr` omitted native's zero-GC-count return and indexed storage that need not exist for a non-GC layout. Queries beyond inline storage threw instead of reporting `TYPE_GC_NONE`. | Retain the slot-bound assertion, then return `TYPE_GC_NONE` when the layout has no GC pointers, as native does. | Corrected with the slot-count prerequisite; GC-bearing layouts still read their recorded slot classifications. |
| B172 | Port defect / per-local single-definition diagnostics | `lvSingleDefDisqualifyReason` was a constant read-only `'H'`, preventing reference accounting from recording native zero-initialization and multiple-definition reasons. | Store the Debug reason per descriptor while preserving native `'H'` for default-initialized values and arrays. | Corrected with reference accounting. XOR encoding preserves all byte values, independent locals, struct copies and zero initialization. |
| B173 | Port defect / fatal compilation results | `fatal(CorJitResult)` discarded its result, and the outer handler always reported an internal error. The unfinished lowering driver also threw an ordinary managed exception across the AltJIT ABI. | Carry the native result in `FatalJitException`, report and return it at the compilation boundary, and explicitly skip unfinished lowering through that failure path. | Corrected. The native-host corpus first aborted at lowering, then exposed duplicate fallback attempts when every result became an internal error. It now captures each selected method once and returns to native fallback; this is not managed code generation. |
| B174 | Port defect / tracked-local bitset dimensions | `TrackedVarBitSetTraits.GetArrSize` returned the element count while `GetSize` returned the word count. This allocated incorrectly sized storage and constructed the wrong full set. | Restore the native word-count and element-count getters together. | Corrected with tracked-local selection. Boundary cases cover one word, a full word, and multiple words in both configurations; existing bitset, reference-accounting, assertion, block-morph and loop consumers retain their behavior. |
| B175 | Upstream diagnostic defect / sort verification | The pinned `jitstd::sort` Debug postcondition increments its iterator but repeatedly checks only the first adjacent pair (`jitstd/algorithm.h:193-196`). | Record separately from sort behavior; do not silently strengthen only the managed diagnostic. | The tracked-local sort retains native comparison and pivot order, including this check. No runtime sorting defect is established by the diagnostic omission. |
| B176 | Port defect / LSRA register iteration bounds | Register walks treated `availableRegCount` as the length of the compact non-APX register map, but native uses it as an exclusive register-number bound. EVEX initialization indexed beyond the map; legacy mapped walks visited unavailable high vector registers. | Initialize physical records with native consecutive register numbers; terminate mapped reset/free/allocation walks using the mapped register number, not its array index. | Corrected in physical-record initialization, register-lifetime helpers and minimal allocation traversal. Explicit legacy and EVEX cases exercise initialization, encoding order, reset and free verification; the EVEX case failed before the correction. |
| B177 | Port defect / memory-kind set packing | BasicBlock's five memory-use/def/live/havoc fields were one-bit `MemoryKind` values instead of native two-bit `MemoryKindSet` masks, losing the second memory kind and combined sets. | Use the existing set representation and native field widths while preserving neighboring fields and unused storage bits. | Corrected with liveness initialization. All four masks round-trip independently; setters preserve native truncation, and initialization clears use/def/live sets without clearing havoc or memory SSA state. |
| B178 | Port defect / unary initialization classification | `GenTree.IsCnsInitVal` read `GT_INIT_VAL` through the binary `GenTreeOp` subclass, rejecting correctly represented unary nodes. | Read the operand through `GenTreeUnOp`, preserving native constant-versus-variable classification. | Corrected. Both wrapped constants and wrapped nonconstants exercise the actual managed unary representation. |
| B179 | Port defect / zero-fill helper unwrapping | The block-helper draft used the value predicate `IsInitVal` to decide whether to unwrap a node. That predicate also accepts an unwrapped zero constant, so large zero fills attempted an invalid unary cast instead of creating a memzero call. | Unwrap only the `GT_INIT_VAL` operator, matching native `OperIsInitVal`. | Corrected in the pending lowering packet. Actual helper lowering preserves full unsigned sizes and volatile barriers for zero, fill and copy blocks; driver activation remains separate. |
| B180 | Port defect / return-buffer argument unwrapping | `gtCallGetDefinedRetBufLclAddr` treated register and stack argument wrappers as binary `GenTreeOp` nodes, rejecting their unary CLR representation. | Unwrap through `GenTreeUnOp` so physical call definitions remain attached to their actual local-address nodes. | Corrected with per-block liveness generation, including real register and stack argument wrappers. |
| B181 | Port defect / empty bitset assignment | An empty span represents both an uninitialized bitset and a valid zero-element set. `BitSetOps.Assign` rejected the latter when copying into empty storage. | Allow the empty representation when the traits report zero elements; retain copy semantics and validation for nonempty environments. | Corrected in the canonical assignment operation. Zero-local liveness no longer needs a special bypass. |
| B182 | Port defect / initialization-wrapper traversal | The lowering draft rejected `GT_INIT_VAL` before visiting the block store that consumes it, although native leaves the wrapper unchanged at that point. | Preserve the wrapper during node traversal and let common block lowering consume or contain it. | Corrected in the pending lowering dispatcher. Whole-block traversal handles initialized stores in minopts and optimized modes and continues after scalar-store replacement. |
| B183 | Suspected upstream defect / LSRA candidate narrowing | At the pinned `lsrabuild.cpp:3883-3898`, the non-RMW second-operand contained-indirection path with nonempty candidates and no EVEX builds the first operand again and leaves the second operand's candidates unchanged. | Establish reachability and compare upstream behavior before changing candidate narrowing or use construction. | Preserved in the managed port. Source inspection only; no native runtime failure or approved behavior change is claimed. |
| B184 | Validation defect / target-guarded tests | The core test project did not receive the implementation's target and feature symbols, silently excluding hardware-containment fixtures and xarch assertion paths. The excluded assertion code still used pre-reconciliation SSE2 value-number names. | Obtain compilation symbols from the referenced core project and use current generated intrinsic names. | Corrected without maintaining a separate feature list. Target-guarded hardware cases are discovered, and the restored assertion paths pass in Debug and Release. |
| B185 | Port defect / LIR subrange enumeration | Forward and reverse `ReadOnlyRange` enumerators ignored the opposite endpoint and traversed the rest of the containing block. Containment of newly inserted temporary nodes consequently reached an outer, not-yet-lowered hardware helper. | Capture the native exclusive end node in each direction and stop there. | Corrected in the canonical enumerators. Four subrange cases fail before the fix; construction lowering no longer visits nodes outside its requested range. |
| B186 | Port defect / SIMD selector operand contracts | Binary and comparison selectors still required exact SIMD operand types after lowering elided floating scalar construction and width-changing reinterprets. This rejected native-valid Dot reductions and could select ARM64 scalar multiplication for a full-vector LIR operand. | Match the pinned native full-vector predicates and floating-scalar allowances without retagging operands or changing memory-access widths. | Corrected in both selectors. Four scalar/reinterpret cases fail before the fix; exact-width cases retain their behavior. Non-LIR comparison operands retain the stricter type contract. |
| B187 | Port defect / intrinsic auxiliary-type initialization | `GenTreeJitIntrinsic` left its auxiliary type at managed zero (`TYP_UNDEF`) instead of native `TYP_UNKNOWN`. Lowering queries that distinguish absent auxiliary metadata consequently observed the wrong type. | Initialize the field in the shared intrinsic constructor and retain independent SIMD element-type metadata. | Corrected; all four metadata cases fail with the initialization removed. The native register-type view maps `TYP_UINT`/`TYP_ULONG` to `TYP_INT`/`TYP_LONG` without changing the stored element type. |
| B188 | Port completion / emitter encoding eligibility | The temporary broadcast query used compiler ISA availability rather than the emitter's selected encoding mode. The promoted-EVEX setter also rejected the independent promoted-only mode supported by the native query. | Route broadcast eligibility through the canonical emitter/instruction queries and preserve independent vector/promoted EVEX state. | Corrected, retaining instruction-specific ISA checks. The disabled-emitter broadcast case fails before migration; promoted-only encoding accepts integer/mask instructions without enabling vector broadcast. Redundant generated boolean tables are removed. |
| B189 | Port defect / lowering traversal | The pending dispatcher rejected native passthrough operators, omitted optimized bitwise pretransforms, and ran byte-swap lowering from containment traversal rather than node lowering. | Restore native dispatch, transformation order and containment-only behavior. | Corrected; ten dispatcher cases fail before the fixes and pass afterward, covering bitwise folding, all-ones-mask ownership, existing compare-jump nodes and byte-swap traversal. |
| B190 | Port defect / memory RMW classification | The contiguous operator range ended at `GT_SUB` instead of `GT_ROR`, excluding bitwise operations and shifts/rotates. AND-NOT lowering consequently replaced a valid memory read-modify-write form. | Match the native operator set without changing recognition or transformation priority. | Corrected; coverage exercises all eight excluded operators and preserves the AND-NOT memory form. |

| B191 | Port defect / final-block removal | `fgRemoveBlock` asserted a non-null successor even when removing the final unreachable block, contrary to native `fgbasic.cpp:5231`. | Return a nullable successor and preserve native block-removal diagnostics. | Corrected; lowering orchestration removes a final unreachable LIR block with either an existing or newly computed DFS tree. |
| B192 | Port defect / contained call register diagnostics | Call containment cleared the register tag instead of assigning native `REG_NA`; restoring the tag exposed missing sentinel handling in register-name lookup and aborted native-host dumping. | Assign the native no-register value and special-case its name as in `utils.cpp:132`. | Corrected; contained-call and register-name regressions cover the tag, sentinel and real-register names. Native-host captures complete without the out-of-range lookup. |
| B193 | Port defect / lowering identity and P/Invoke preparation | Switch guard operands and GC-state stores allocated nodes in a different order; P/Invoke frame addresses used byref instead of native integer type, and method-prolog range diagnostics were omitted. | Preserve Windows native construction order, native frame-address types and prolog dump sites. | Corrected; switch and GC-store ID assertions accompany exact native lowering phase/IR matches for switch, direct-call and P/Invoke corpus methods. |
| B194 | Port defect / local display padding | The native local-name display width includes its terminating NUL. Managed code compensated only for shorter names, omitting the separator after `PInvokeFrame`. | Use the native width and pad by the exact difference. | Corrected; P/Invoke and main lowering phase sections and final IR now match native without whitespace normalization. |
| B195 | Port defect / SIMD width input range | `roundDownSimdSize` accepted signed `int` even though native `compiler.h:10543` takes an unsigned block size. LSRA block requirements must not narrow a high-bit size or compute its remainder as negative. | Preserve unsigned input and block-size arithmetic; retain the bounded SIMD-width result and positive-size callers. | Corrected with call/memory reference construction. Init and copy cases above `INT_MAX` preserve the native SIMD and remainder temporary requirements. |
| B196 | Code placement / call encoding queries | LSRA initially contained the native varargs-register and vzeroupper queries and the fast-tailcall specialization of GS-cookie temporary selection. Code generation needs the same contracts. | Reuse these implementations when their additional backend callers are ported; move them to their canonical native owners rather than adding parallel classifiers. | The complete xarch GS-cookie selector now belongs to `CodeGen`, shared by call constraints and interval construction. Varargs-register and vzeroupper queries remain in LSRA; source mappings identify `compiler.hpp:3097-3116` and `gentree.cpp:1920-1997`. No behavior change is proposed. |
| B197 | Upstream cleanup candidate / local-load dispatch | Native `lsraxarch.cpp:97-109` tests `GT_STORE_LCL_FLD` inside the `GT_LCL_VAR`/`GT_LCL_FLD` switch section, making that SIMD12 temporary branch unreachable. | Reconcile this stale branch with upstream before removing or relocating it; do not introduce extra register requirements during translation. | Preserved in the managed dispatcher. No generated-code defect is established. |
| B198 | Deferred target defect / multireg classification | The existing `GenTree.IsMultiRegNode` branch under `FEATURE_MULTIREG_RET && TARGET_32BIT` calls nonexistent `OperIsMultiRegOp()` rather than the managed `Oper.IsMultiRegOp` property. | Correct the classifier when validating the 32-bit target; the new destination-count helper already uses the existing property. | Source-only finding outside Windows-x64 execution; no 32-bit validation or behavior change is claimed. |
| B199 | Port completion / multireg intrinsic metadata | `HWIntrinsicInfo.IsMultiReg` still returned `false`, preventing DivRem and BigMul from exposing both native result registers. | Use the existing generated `HW_Flag_MultiReg` flag rather than a separate classifier. | Implemented with node-reference construction. The worker's two DivRem cases and contained-memory BigMul case failed against the stub; dispatcher coverage also verifies both result definitions. |
| B200 | Upstream documentation discrepancy / copy-reload placement | The leading `insertCopyOrReload` description in `lsra.cpp:7276-7331` says reloads are inserted immediately before the parent, but its implementation calls `InsertAfter(tree, newNode)` at line 7400. | Preserve the pinned executable ordering; reconcile the stale explanation upstream before using it to justify a placement change. | Managed insertion follows the implementation, with an intervening sibling explicitly covered. No native generated-code defect is established. |
| B201 | Port bug / allocation diagnostics | Shared allocation diagnostics invented `BB01 <- BB00` rows, uppercased event register names and widened node-location columns using non-node references. Native LSRA uses specialized `BB1 PredBB0` rows, lowercase register names and `maxNodeLocation` alone. | Restore the pinned allocation-table formatting shared by allocation and final verification. | Corrected with minimal resolution. An exact block-row regression crosses a non-node decimal boundary and fails before the fix; allocation, reuse, copy and spill-event assertions retain native casing. |
| B202 | Port bug / register-mask bank selection | LSRA's mask helper accepted only canonical `TYP_INT`, floating-point and mask types, rejecting long, GC-pointer and SIMD intervals during real allocation. | Use native `regMaskTP::GetRegSetForType`: select the low or high bank without filtering the contained register bits. | Corrected during allocation activation. A direct long-return allocation fails before the fix; data-driven bank checks cover integer, GC, floating, vector and mask types. |
| B203 | Port bug / register-availability aliases | Allocation updated availability by raw value type while freeing used the physical register class. Long/pointer and float/double aliases could therefore disagree about whether the same physical register was occupied. | Index availability through the existing canonical `regType` mapping for assignment, release and queries. | Corrected during allocation activation. Six mixed-type allocation/free cases fail before the fix; the corrected native-host corpus completes allocation for all twenty methods. |
| B204 | Port completion / frame and GC options | The `fgSetOptions` stub omitted required frame-pointer and interruptibility policy. Allocation's P/Invoke frame diagnostic exposed the missing earlier policy, including the EH requirement to report roots in aborted frames. | Port the complete native option selection and codegen frame/GC setters rather than hiding the later diagnostic. | Completed with allocation activation. Data-driven frame-policy checks cover EH interruptibility, P/Invoke, localloc, profiling, debugging and frame-pointer omission; the full P/Invoke allocation dump now matches native. |
| B205 | Port bug / allocation block reset | Minimal allocation called its traversal-start reset at block boundaries, leaving retained integer and floating constants assigned and preserving stale next-reference/spill-cost state. | Call the existing native `resetAllRegistersState` from the no-enregistered-local block-end path. | Corrected with allocation activation. Both constant-retention regressions fail before the fix; the complete multi-block `ManyReturns` allocation dump now matches native. |
| B206 | Port bug / Release GC feature selection | `EMIT_GENERATE_GCINFO` was defined only in Debug, although the pinned native target headers require it in every configuration. Release would silently omit pointer-register updates and eventual GC metadata generation. | Define the feature for all configurations; do not suppress the new Release diagnostic or make its state update a no-op. | Corrected with code-generation preparation. A reference-to-byref transition followed by register death fails in Release before the project-definition fix and passes afterward with normal analyzers enabled. |
| B207 | Porting decision / emitter descriptor storage | Native `emitGenIG`/`emitSavIG` allocate a packed temporary descriptor buffer, copy it into group-owned storage with GC metadata, and relocate jump/alignment/last-instruction pointers. The C# descriptor hierarchy consists of managed classes and cannot be copied into that byte buffer. | Retain typed managed descriptors in group-owned collections with explicit native logical sizes/offsets. Preserve group splitting, ordering, GC snapshots and every saved descriptor reference; do not manufacture sizes or silently skip relocation-dependent behavior. | Implemented for group preparation/saving, allocation/overflow transitions and basic/jump/alignment layouts under the direction to proceed autonomously. Group-owned arrays preserve native logical capacity, GC headers, list order and saved aliases; emitted-code patch addresses remain native pointers. Additional descriptor families and encoding remain. No codegen or dump differences are accepted. |

| B208 | Port defect / legacy local register masks | `LclVarDsc.lvRegMask` shifts a raw 64-bit value by the absolute register number. It does not preserve the high register bank, and mask-register indices can alias integer bits. | Migrate remaining consumers to native bank-aware masks when their behavior is ported; reuse `CodeGen.genGetRegMask(in LclVarDsc)` rather than introducing another classifier. | New local spilling uses the bank-aware helper, with a K1 spill regression preserving an unrelated RCX GC root. The legacy property and unrelated consumers remain deferred; no broad register-mask audit is claimed. |

| B209 | Port defect / sorted scope cursors | `compEnterScopeList` and `compExitScopeList` ignored their sorted index arrays, exposing descriptor input order to debug basic-block creation and codegen scope opening. | Dereference the appropriate permutation in each accessor; sorting comparators must compare raw descriptors rather than consulting the permutation being sorted. | Corrected. The cursor-order/ref-identity regression fails before the fix; unordered-scope basic-block and IL-gap coverage exercise both consumers. The minopts native-host corpus has debug code disabled and does not establish scope parity. |

| B210 | Upstream question / filter-entry loops | Native `fgCreateFuncletPrologBlocks` checks handler heads for backedges but explicitly omits filter heads, noting this might be a bug and questioning whether the entry exception object makes such a loop illegal. | Preserve the pinned behavior; establish legal IL and native behavior separately before changing filter prolog placement. | Recorded while porting the complete funclet-creation phase. No executable defect is established and no managed-only change is made. |
| B211 | Upstream question / reload GC state | Native `genUnspillLocal` (`codegenlinear.cpp:1162-1171`) directly adds the register-variable mask instead of calling `genUpdateRegLife`, because LSRA resolution can leave the register marked live. Its TODO suspects a GC hole. | Preserve the native workaround; investigate with a concrete optimized allocation/resolution repro before changing register-home or GC-root ordering. | The managed reload port retains the same mask update. No executable defect or output deviation is established. |
| B212 | Upstream question / multi-register local indices | Indexed native `genRegCopy` (`codegencommon.cpp:8402`) calls the unindexed `genUpdateVarReg`, while indexed `genConsumeReg` (`codegenlinear.cpp:1521-1522`) passes the parent local number to `genSpillLocal`. These warrant checking when later fields change register or spill. | Establish the relevant LSRA invariants and an executable native repro before replacing the primary register or parent home with a field-specific one. | Both callsites are preserved as pinned. Production allocation still rejects enregistered locals; the current copy-order test covers a first-field copy followed by a second-field reload, not these suspected index-loss cases. |
| B213 | Upstream question / tagged data-offset range | Native `eeFindJitDataOffs` accepts raw offsets below `0x40000000`, but `eeGetJitDataOffs` asserts that the encoded handle is below that limit and uses a signed right shift (`ee_il_dll.cpp:1422-1459`). High encoded values therefore decode negatively in Release. | Establish the actual constant-section size invariant before changing constructor or decoder limits. | The port preserves both assertions and the signed decoder. Boundary tests retain Release behavior; no executable large-method defect is established. |
| B214 | Upstream question / SIMD12 constant padding | Native `genSetRegToConst` builds a zero-padded `val16` temporary for SIMD12, but passes the original `val` to `emitSimdConstCompressedLoad` (`codegenxarch.cpp:331-334`). | Establish the inactive-lane invariant and an executable native repro before substituting the padded value. | The original-value callsite is preserved. A direct helper test retains a nonzero fourth lane; no defect in normal compiler-generated SIMD12 nodes is established. |
| B215 | Upstream question / APX address-operand format | Native `emitIns_R_R_A` omits `useNDD` from format selection (`emitxarch.cpp:8395`) but sets EVEX.ND at 8402. Its stack counterpart passes `useNDD` at 8586. For NDD ADD this yields a read/write address-form destination versus a write-only stack-form destination. | Establish whether the descriptor classification affects generated code or downstream analyses before changing it. | The port preserves the pinned distinction. No executable defect or managed/native output difference is established. |

| B216 | Port defect / register-copy target guards | `instr/CodeGen.RegisterCopies.cs` closes the inner `TARGET_AMD64` guard before `emitIns_Mov`, so its following `#else` pairs with the file's `TARGET_XARCH` guard instead. AMD64 compiles, but x86 loses the `ins` declaration and non-xarch preprocessing leaves unmatched C# scope. | Repair the target guards with a targeted non-AMD64 compile check when restoring other-target compilation; do not infer support from the active AMD64 build. | Discovered while reusing register copies for local stores. No AMD64 behavior change is required; unrelated target repair is deferred. |

| B217 | Upstream question / contained local-address load lifetime | Native `emitInsLoadInd` updates the contained `GT_LCL_ADDR` after recording a load (`emitxarch.cpp:6292-6298`). Its TODO questions updating the indirection instead, as the store counterpart does for a GC-hole fix. | Preserve the pinned call and establish a concrete native lifetime/GC repro before changing the node used for the update. | Recorded during indirect-load generation. No executable defect is established and no managed-only algorithm change is made. |

| B218 | Upstream question / absolute indirect-call assertion | Native `emitIns_Call` (`emitxarch.cpp:11129-11143`) checks `params.addr` for signed-32-bit encodability in the no-base/no-index address-mode branch, although that call form requires `params.addr == nullptr` and carries the address in `params.disp`. | Confirm the intended assertion operand upstream; preserve the pinned code until a correction is approved. | The vacuous native assertion is retained. Relocation selection still tests the actual displacement; no executable defect or managed-only behavior change is established. |

| B219 | Port defect / AMD64 frame deltas | Managed `genCallerSPtoFPdelta` negated the native sum, producing a positive caller-relative offset, and `genSPtoFPdelta` inverted the native EnC predicate. Both affect the profiler's caller-SP argument. | Restore the pinned AMD64 formulas (`codegenxarch.cpp:7203-7308`) before using them in return/profiler generation. | Fixed with regression coverage for ordinary/EnC frames, localloc precedence and its 240-byte cap, SP/FP-relative offsets and OSR root-frame adjustment. No upstream algorithm change. |
| B220 | Suspected upstream defect / compaction phi splice | When both blocks have phis and the target has only phis, `fgCompactBlock` overwrites `targetFirst->Prev` with `blkLastPhi` before using it as `targetLastPhi` (`fgopt.cpp:945-963`). The target's phi prefix is then disconnected instead of appended. | Establish upstream reachability and resolve upstream before changing port behavior. | Pinned behavior preserved with two focused cases covering a source with and without ordinary statements; no C#-only algorithm correction. |
| B221 | Port defect / block emptiness | Call-finally-pair and empty-block removal assertions and branch-cycle detection used inherited `LIR.Range.IsEmpty`, which neither scans statement blocks nor ignores LIR IL-offset markers as native `BasicBlock::isEmpty` does. | Port complete mode-specific emptiness (`block.cpp:1016-1040`) and use it in compaction, the pair assertion, `ehCanDeleteEmptyBlock`, `fgRemoveBlock` and branch threading/cycle detection. | Fixed with IL phi/NOP and LIR marker/executable-node cases. The nonempty-statement cycle regression fails before correction; inherited range emptiness remains unchanged for range operations. |

| B222 | Port defect / graph-dump buffer reuse | `fgDispBasicBlocks` assigned `List.Capacity = fgBBcount` before clearing the previous order. Unlike native `reserve`, this attempts to shrink below the previous count after block removal and throws. The optimized native-host corpus failed immediately after the newly activated early graph-update phase. | Use `EnsureCapacity` to preserve native non-shrinking reservation, then clear and repopulate the list. | Corrected with a graph-compaction/dump regression that reproduces the original exception before the fix. |
| B223 | Port defect / optimized implicit-byref replacement | The optimized `ImplicitByRefArgument` corpus case asserts in the `GenTree` replacement constructor during global morph: a linked source is passed with `NodeThreading.None`. The same assertion reproduces with the previously published local funclet snapshot, before full graph-update activation. | Reconcile the implicit-byref access replacement's threading/owner contract, preserving logical identity and local-threading aliases. | Recorded outside the active graph-driver batch. All nineteen early graph phases complete before this preexisting failure; the optimized corpus is not a successful full run. |

| B224 | Port defect / epilog ABI register-mask check | `CodeGen.CallTargets.cs` converts `segment.RegisterMask` directly with `regMaskTP.CreateFromRegNum` when checking registers trashed by an epilog. The ABI mask is relative to its integer/float/mask bank, whereas `regMaskTP` keeps integer and floating bits at their native register positions in one lower word. Floating arguments can therefore test unrelated integer bits. | Reconcile the epilog diagnostic with native register bits, retaining the ABI mask's existing enumeration contract for other consumers. | Identified while integrating JMP argument placement, whose new register accounting uses `segment.Register.SingleTypeMask`. Existing epilog diagnostic repair is deferred; no unrelated generated-code change. |

| B225 | Upstream question / immediate-only opcode table | Native `emitIns_I` includes `INS_jge` (`emitxarch.cpp:7374`), but `instrsxarch.h` declares `jge` with `INST0`, outside the MI opcode-table prefix used by `insCodeMI`. | Establish whether this case is reachable and resolve upstream before changing the opcode selection. | Pinned switch and table behavior preserved. Local heap generation uses `INS_push_hide`, not this case; no successful `jge` immediate path is claimed. |
| B226 | Port bug / atomic address liveness | `TreeLifeUpdater.UpdateLife` and `IndirOrArrMetaDataAddr` required `AsIndir()` for all indirect operators, but ordinary atomic nodes are `GenTreeOp`; native layout compatibility does not imply CLR subtype identity. | Access the shared address operand without changing node representation or native local-address handling. | Fixed in shared address access and liveness. A Debug XADD regression fails before the fix; atomic generation and both local-address handling modes pass afterward. |

| B227 | Upstream question / vector-division emitter arguments | Native `hwintrinsiccodegenxarch.cpp:2500-2555` passes `instOptions` into `emitIns_Mov` boolean parameters and `emitIns_R_C` displacement parameters. The corresponding managed unsigned vector-division paths preserve those conversions. | Audit the native argument positions separately before changing them; clarify intent without silently changing the port's instruction selection or copy elision. | Preserved. The current base-family callsite supplies `INS_OPTS_NONE`; no nonzero-options execution defect is established. |
| B228 | Port bug / alignment descriptor opcode | `emitNewInstrAlign` allocated an alignment descriptor but omitted the native `emit.h` assignment of `INS_align`. Loop recording failed its final opcode assertion; Release could misclassify the descriptor during group processing. The original allocation test assigned the opcode itself, hiding the omission. | Initialize the opcode in the wrapper and exercise allocation without repairing the descriptor in the test. | Fixed with block-driver integration; final alignment and encoding remain separate work. |

| B229 | Port bug / AMD64 OSR frame delta | `CodeGen.genSPtoFPdelta` imposed a frame-pointer precondition on AMD64, unlike native `codegenxarch.cpp:7203-7244`. OSR uses that calculation while reconstructing Tier0 frames without a frame pointer. | Retain the precondition only for targets that require it; reuse the native AMD64 calculation. | Corrected with prolog materialization; covered by the OSR stack-pointer-frame regression. |

| B230 | Port bug / saved-current instruction traversal | Managed `emitPrevID` selected the recording buffer whenever its group equaled `emitCurIG`. Prolog/epilog materialization leaves the saved final group current after clearing that buffer, so final backward validation indexed an empty list. Native descriptors remain addressable after saving. | Use the active buffer only while it contains the current group's recording; otherwise use the group's saved descriptor snapshot. | Corrected with prolog/epilog materialization; the two-epilog integration regression exercises saved-current backward traversal. |

| B231 | Port prerequisite / invalid instruction-format diagnostics | Native register/immediate and memory byte output print `emitDispIns(id, false, false, false)` before invalid-format assertions. | Retain explicit invalid-format failures and the preceding native descriptor dump; do not treat byte-output tests as full dump parity. | Resolved with instruction display and restoration of the eight deferred address/register/immediate/stack/static diagnostic calls. Production emission remains unavailable. |

| B232 | Port bug / prefix mutation during opcode composition | A C# compound assignment such as `code |= insEncodeReg012(..., &code)` reads `code` before the helper adds REX bits, then overwrites that mutation. Register push/pop and immediate output therefore lost the R9 prefix. | Evaluate the register-encoding helper first, then combine its returned bits with the updated opcode. | Corrected across the new opcode-register, MI and mov-immediate output paths; exact R9 push/pop/add/mov byte regressions retain the prefix. |
| B233 | Port bug / signed machine-address arithmetic | Checked C# pointer arithmetic throws for valid negative writable-alias offsets, static-data displacements and hot/cold address deltas. Primitive output and relocation callbacks had only exercised positive aliases. | Use narrowly unchecked native-width pointer expressions while retaining displacement bounds and relocation policy. | Corrected in primitive writes, relocation-address formation, static/branch/padding output and instruction hex display. Four negative-alias writes, two relocation cases and the negative-alias diagnostic case fail before their respective fixes; byte-output, negative data-offset and cross-region regressions cover the corrected paths. |
| B234 | Port bug / spill-temp diagnostic variable numbers | Native stack output passes signed negative spill-temp numbers to the GC updater's unsigned diagnostic parameter. A checked C# cast throws before the updater applies the native tracking policy. | Preserve the native modulo conversion with `unchecked((uint)varNum)`, without promoting untracked temps into tracked local lifetimes. | Corrected in `emitOutputSV`; the GC-typed spill-store regression covers bytes and absence of a fabricated stack lifetime. |
| B235 | Port bug / compact GC stack mask pop | The initial `emitStackPop` translation cast signed compact masks to `uint` under checked arithmetic. A live argument in bit 31 made that conversion overflow instead of performing native logical right shift. | Reinterpret both reference and byref masks without overflow checking before shifting; preserve the native pop count and stack accounting. | Corrected for both masks. The highest-bit reference and byref cases fail before the fix and pass afterward. |
| B236 | Upstream question / data relocation writable aliases | Native `emitOutputDataSec` passes addresses in `blockRW` to `emitRecordRelocation`, whose common wrapper adds the current code `writeableOffset` (`emit.cpp:8494`, `10576`). The driver calls data output at `7596` without an apparent offset reset. | Establish whether runtime relocation handling makes this valid for nonzero code aliases before changing the port. Preserve native behavior meanwhile. | Nonblocking question; no runtime failure established. The current data tests use a zero code alias offset. |
| B237 | Upstream configuration question / non-debug alignment scoring | `getInsExecutionCharacteristics` is enabled by `DEBUG` or `LATE_DISASM`, but its alignment case reads `isPlacedAfterJmp`, whose declaration and initialization are `DEBUG`-only (`emitxarch.cpp:20090`, `emit.h:2315`, `emit.cpp:5813`). `jit.h:326-332` enables late disassembly by default only in Debug but describes optional non-Debug use. | Establish the intended metadata contract before enabling non-Debug late disassembly with loop alignment. Do not approximate the missing placement state. | Default Debug/Release configurations are unaffected. The managed non-Debug late-disassembly path explicitly rejects nonempty alignment scoring rather than guessing its cost. |
| B238 | Port prerequisite / emission CSE metrics | Native `genEmitMachineCode` calls `optGetCSEheuristic` for `opts.dspMetrics`; the selector constructs one of the native CSE policies when no object exists (`optcse.cpp:5566-5655`). The managed heuristic hierarchy is still an empty shell. | Port the selector, policy construction and metric reporting before enabling this existing diagnostic mode. Do not substitute null or empty metrics. | D008 explicitly rejects the mode before emission mutation, unwind reservation and EE allocation. Ordinary emission does not require it; production generation remains gated on runtime metadata. |
| B239 | Upstream ownership question / async diagnostic buffers | Native `genReportAsyncDebugInfo` prints host-array contents after transferring their ownership to the EE (`codegencommon.cpp:7233-7248`; `corinfo.h` callback contract). The pinned CoreCLR callback retains them (`vm/jitinterface.cpp:11697`), but the interface does not require other hosts to do so. | Preserve callback and diagnostic ordering while reading the original managed records for diagnostics, not transferred buffers. Verify that callback-side buffer reuse cannot corrupt managed output. | Managed publication preserves record values and prints them after the callback. Debug/Release coverage poisons transferred arrays and verifies the original diagnostic values, resume offsets and unsigned ABI carriers. No pinned CoreCLR output difference is intended. |
| B240 | Port bug / fat GC headers with no interruptible ranges | The initial GC encoder translation emitted the range count only for a nonempty range list. Native emits it for every fat header, including zero (`gcinfoencoder.cpp:1111-1117`). | Use the exact slim/fat decision for field presence and verify streams against a native byte oracle, not fixture values derived from the translation. | Fixed. Ten fixture streams match the pinned native encoder. A byte-sensitive fat-header/untracked-slot regression fails with the old predicate and passes after restoration; the slim case passes both. The empty fat-header case alone can hide omitted zero bits in padding. |
| B241 | Port prerequisite / late-disassembly callback ABI | CoreDisTools uses variadic print callbacks. Its buffered interface matches tested valid instructions but omits returned decoder-error text and writes errors to stderr instead; native RyuJIT writes all callback levels to its selected stream (`disasm.cpp:1572-1590`). | Obtain approval for a native callback bridge or another exact ABI solution. Do not reconstruct decoder-internal error details or accept different output. | D009 explicitly rejects requested late disassembly before phase mutation/publication. Ordinary emitter disassembly remains separate. Native comparison evidence and the deferred buffered draft are retained outside maintained source. |
| B242 | Port bug / GC chunk transition ordering | The initial chunk encoder sorted transitions only by slot. Native `CompareLifetimeTransitionsBySlot` also orders equal-slot entries by code offset (`gcinfoencoder.cpp:838-865`). An unstable managed sort can reorder dense same-slot transitions and change their encoded offsets. | Preserve both comparison keys and verify a dense chunk against the native encoder, including a failing run with the slot-only comparator. | Fixed. A native 41-byte fixture interleaves 36 transitions across three slots in one chunk. The old comparator fails at byte 10; the two-key ordering matches native in Debug/Release and preserves the duplicate-transition assertion. |
| B243 | Port activation / completed root compilation status | Activating `genGenerateCode` also requires removing the obsolete unconditional root `CORJIT_SKIPPED` ending in `compCompileHelper`. Native returns `CORJIT_OK` after its matched-VM and requested AltJIT-execution guards (`compiler.cpp:7135-7157`). | Preserve those native guards and report success after complete code and metadata publication. Verify actual execution, not only emitted dumps. | Fixed. The stale skip led to a native fallback assertion recursion and stack overflow after emitting Main. Returning the native success status enables all twenty minopts corpus methods to execute with exit zero; the bounded first-exception probe and before/after captures are retained as evidence. |
| B244 | Port diagnostic parity / executing backend corpus | First complete backend captures exposed empty-bitset hexadecimal omission, zero-padded struct sizes and non-diffable unwind offsets, followed by extra block IDs in mapping output and raw addresses in `allocUnwindInfo` diagnostics. | Match native formatting at its source rather than normalizing dump differences. Keep raw relocated code bytes separate from diagnostic-text parity. | All five roots are corrected with focused coverage. Fresh GCStress4 captures match all 137 block-mapping lines and 22/23 unwind-allocation lines; the remaining unwind line reflects the separate hardware-import/code-size difference. Generation estimates (B245) and raw address-bearing code remain unmatched. |
| B245 | Suspected upstream defect / minopts diagnostic estimates | Native `compiler.cpp:4972-4987` initializes cycle/size estimates only when verbose optimization is enabled, but `codegenlinear.cpp:197-205` prints them whenever verbose. Minopts captures print `0xCDCDCDCDCDCDCDCD` as an unsigned decimal value; managed fields remain zero. | Preserve the native control predicates without fabricating poisoned managed fields or silently fixing the native diagnostic. Keep this exact-text mismatch visible pending an upstream resolution or an explicitly accepted difference. | This is the first generation-phase difference in 19/20 fresh corpus methods; the hardware case differs earlier. No generated-code difference is attributed to these diagnostic fields. |
| B246 | Suspected upstream defect / hash-vector XOR insertion | `XorAction::LeftGap` at `hashbv.cpp:1051` links the new node to `(*l)->next`, then replaces `*l`, dropping the existing live node without decrementing `numNodes`. A one-bucket left vector containing bit 128 and right vector containing bit 0 reaches this path. | Retain the native body and defer vector XOR pending upstream resolution or explicit approval of a behavioral difference. Do not substitute a corrected managed algorithm. | Vector XOR remains unported. Independent node-level XOR and unaffected vector set operations are implemented; their validation does not cover the disputed vector operation. |
| B247 | Suspected upstream defect / hash-vector intersection progress | `IntersectsAction::BothPresent` at `hashbv.cpp:1228-1235` does not advance either cursor when shared nodes have disjoint bits. `MultiTraverseEqual` calls it repeatedly for left bit 0 and right bit 1 in the same node. | Retain the native body and defer vector intersection pending upstream resolution or explicit approval. Do not introduce a managed-only cursor advance or run an unbounded regression. | Vector `Intersects` remains unported. Node-level `Intersects` is independent and implemented; no corrected vector behavior is claimed. |
| B248 | Port defect / GC-qualified LEA diagnostics | Managed `emitDispIns` applied LEA's ordinary-size assertion and qualifier removal to GC-reference and byref descriptors. Native `emitxarch.cpp:12491-12518` applies that special case only in the non-GC branch. Array interior-address generation aborted when verbose output selected `EA_BYREF`. | Preserve the native branch predicate and retain `gword ptr`/`bword ptr` qualifiers without changing descriptor GC metadata. | Fixed. Before the correction, both GC-qualified regression cases failed while ordinary 32/64-bit LEAs passed. Final instruction-display tests pass in Debug/Release; fresh managed execution reaches the interior-array and pinned-array collection checks. |
| B249 | Port defect / block-header flag truncation | Checked conversions in `dspBlockHeader` threw when flags exceeded a signed low word, including throw-helper blocks reached by array bounds checks. Native `block.cpp:778-781` extracts two unsigned 32-bit words from the 64-bit flags. | Use explicit unchecked unsigned word extraction, preserving every bit and native hexadecimal formatting. | Fixed. Four nonzero high/sign-bit cases failed before the correction; zero passed. Combined diagnostic coverage passes 51 Debug / 41 Release. Field/array byrefs and a pinned-array case now execute across three confirmed full collections under GCStress4; the existing twenty-method corpus also passes. |
| B250 | Port defect / early-liveness statement cursor | The initial early-tree traversal assigned `compCurStmt` for each statement, unlike native `InterBlockLocalVarLiveness`, which clears it for the early policy. A dead store with retained side effects left the cursor nonnull; a repeated pass over emptied blocks could mask the mismatch. | Preserve the native null cursor throughout early backward traversal. | Fixed before integration. The side-effect-retention regression failed before removing the assignment and passes afterward; repeated-removal coverage also checks the cursor. |
| B251 | Port defect / batch temporary initialization | `lvaGrabTemps` repeatedly initialized `lvaTable[tempNum]` while incrementing `lvaCount`. Importer stack merging allocated two locals, leaving the second without a frame home and later failing `raMarkStkVars`. Native `compiler.hpp:2429-2495` initializes each current `lvaCount` entry. | Initialize every newly allocated descriptor at its own index; retain the first index only as the return value. | Fixed. Four multi-temp regression cases fail before the correction and pass afterward, including table growth. The unchanged four-method loop/reference probe now executes managed code through twelve confirmed full collections; existing scalar and interior/pinning cases also pass. |
| B252 | Port defect / identity-folding VN allocation order | The initial binary identity translation hoisted all-bits creation across OR/AND zero exits and omitted or reordered Zero/One interning in arithmetic and comparisons. Equivalent expression results could therefore receive different later VN numbers from native `EvalUsingMathIdentity`. | Preserve native helper calls at their original branch points and in their original order. | Fixed before integration. Fresh-store numbering cases cover the affected paths; marker-based OR/AND regressions fail with the eager all-bits call and pass with native ordering. |
| B253 | Port defect / SSA dead-store replacement metadata | The initial SSA interior-store rewrite created fresh nodes without retaining logical identity or common metadata and omitted native dead-store/tree-modification diagnostics. | Reuse base-only NOP conversion and metadata-preserving COMMA replacement, retain native flag/VN rules, update all owning aliases and restore diagnostics in order. | Fixed before integration. Identity, assertion metadata, flag/VN clearing, aliased-owner and verbose-output cases pass in the SSA-policy suite. |
| B254 | Port defect / composite SSA-number encoding | `SsaNumInfo.Composite` writes newly added slots through an old-length span and copies compact fields using table-relative indices. Reused list storage also needs native value initialization. The DEBUG compact predicate used signed subtraction where native unsigned subtraction rejects SSA numbers zero and one. | Acquire slots after growth, initialize them, copy by field index, and preserve the native unsigned stress predicate. | Fixed. Regressions cover growth with and without reallocation, compact-field preservation, outlined aliases, reset/reuse, the split-index boundary and stress underflow. The complete native composite/read/index helpers are reconciled without activating SSA construction. |
| B255 | Port defect / empty memory-PHI sentinel | Managed `BasicBlock.EmptyMemoryPhiDef` is an uninitialized nullable static field, so it equals the null state for no PHI. Native `block.cpp:995` initializes a distinct nonnull sentinel for a PHI awaiting arguments. | Represent the native sentinel with a distinct managed identity and cover empty versus absent PHI state before SSA construction uses it. | Fixed with a readonly managed singleton. Coverage distinguishes absent, placeholder and populated PHIs, including reset of placeholders without changing the singleton identity. |
| B256 | Port defect / enclosing EH-region output preservation | Native `ebdGetEnclosingRegionIndex` leaves its boolean argument unchanged when neither enclosing region exists. The managed `out` parameter assigns false, changing region-zero insertion eligibility and placing callfinally blocks later in three captured methods. | Preserve the native in/out contract in the helper and each caller; verify insertion choices, resulting branch forms and execution against the pinned oracle. | Fixed. Sentinel and insertion regressions fail before correction. Fresh managed captures match native insertion choices, branch forms, instruction counts and sizes for all three finally methods; five EH/filter methods retain their GCStress4 outcomes. This removes the three-byte differences without claiming full byte identity. |
| B257 | Port defect / memory-kind enumeration | `AllMemoryKinds.GetEnumerator` default-constructs its enumerator instead of invoking the constructor that initializes its cursor before the first element. Enumeration skips `ByrefExposed`, including memory-SSA and liveness consumers. | Invoke the initialized constructor and cover direct, generic/non-generic enumeration and reset against native memory-kind order. | Fixed. Four enumeration regressions fail before correction. Affected liveness/SSA/EH selections pass with both memory kinds, and a fresh NativeAOT JIT executes all 32 established minopts methods under GCStress4 with the reference probes' expected collections and payloads. |
| B258 | Port defect / SSA annotation label formatting | The initial `JitTestCheckSSA` port printed full-width `nint` labels, while native explicitly casts labels to signed `int` for diagnostic output. The mapping keys themselves must remain pointer-width values. | Retain full-width label equality and use unchecked signed-32-bit casts only at native diagnostic sites. | Fixed before integration. Three wide-label cases fail with the old formatter and pass afterward, covering distinct keys with identical printed values and both conflict diagnostics. |
| B259 | Port defect / optimized lowering | `LowerStoreLocCommon` passed a detached block-store node to its diagnostic and returned that node's stale traversal cursor after scalarization replaced it. | Retain `LowerNode`'s returned cursor and derive the diagnostic node from the live range; keep diagnostic-only work out of Release. | Fixed for stores both at the end of LIR and before a successor. Both regressions failed before the correction; focused and integrated Debug/Release coverage and the established 32-method minopts GC-stress execution pass. Fresh FullOpts phase comparison is tracked separately; this does not establish optimized execution parity. |
| B260 | Port defect / obsolete local-thread ownership | The early-phase transition from `AllLocals` to `None` retained local links and statement list heads. Implicit-byref whole-node replacement then correctly rejected the stale links, aborting three optimized corpus methods before SSA. | Retire the obsolete lists at the phase boundary, preserving roots, logical IDs and liveness flags rather than weakening the replacement invariant. | Fixed under D002. Positional and multi-statement cases pass; all three native-host captures now exit successfully and 25 managed compilations finish SSA before the existing optimized-allocation gate. |
| B261 | Missing port / general IR verification | The former `fgDebugCheckLinks` stub omitted native CHECK_IR statement flags/types or LIR links/use-def/local semantics before `fgDebugCheckSsa`; the separate SSA annotation checker was not a replacement. | Port the complete general IR-checking closure and SSA visitor, preserving guards and ordering. Do not insert a reduced SSA-only wrapper or move the call into CHECK_FG. | Complete closure activated. Combined loop/checker captures validate SSA postchecks for 25 optimized methods before the allocation boundary, and 32 minopts methods execute with GCStress4 and no new assertion. Three relaxed `InlineCaller` flag notices match native; 43 additional `GTF_CALL` notices occur on already-divergent `FoldHardware` IR (B111). Recompare those after hardware import/folding agrees; do not weaken checks or claim full diagnostic parity. |
| B262 | Phase integration prerequisite / loop discovery | `fgDfsBlocksAndRemove` clears cached loops, but the former `optFindLoopsPhase` stub did not rebuild them. Activating complete heuristic block weighting therefore encountered missing loop state in the real optimized pipeline despite passing graph tests with prepared loops. | Complete native loop discovery, compaction and canonicalization before publishing the block-weight batch. Do not replace canonicalization with lazy discovery or a no-change phase. | Fixed by the complete loop-phase closure. Eighteen standard optimized methods now pass loop discovery and weighting before reaching the existing allocation boundary; all 32 selected minopts methods still execute under GCStress4. The original failure is retained as before evidence, not a parity pass. |

| B263 | Missing port / loop-alignment placement | Full register allocation reached an enabled-mode rejection in `placeLoopAlignInstructions`. | Preserve native profitability, EH exclusions, lexical placement and hidden-padding rules. | Resolved by complete `shouldAlignLoop` and placement. Selected optimized methods now emit and execute; full code parity remains separate. |
| B264 | Port bug / spill-weight assertion cursor | `getSpillWeight` substituted the current allocation location for native `currentLoc`, which retains its interval-building value during allocation. Checked verification consequently rejected a legitimate delayed local use in `ImplicitByRefArgument`. | Use the actual native cursor; retain the activity assertion and allocation algorithm. | Resolved with a delayed-use regression that failed before the correction. The optimized standard corpus completes managed execution. |
| B265 | Missing port / loop inversion | Native call-free loops inverted while managed `optInvertLoops` was a no-op. During translation, `fgNewStmtFromTree` also introduced threading/cost work absent from native `fgNewStmtAtEnd`. | Port full iteration analysis and inversion; use the exact statement-construction contract. | Resolved. Top/bottom-tested and threading-mode regressions preserve native decisions and unsequenced clones. Optimized execution succeeds; raw code parity is still unestablished. |
| B266 | Upstream diagnostic / loop inversion | `optimizer.cpp:1993-1994` reports a zero-weight in-loop successor but prints the preheader block number. | Confirm and address upstream before changing the managed diagnostic independently. | Preserved as pinned. No algorithmic effect; not a blocker for inversion. |
| B267 | Missing optimization / scalar entry test | After inversion and SSA, native VN gives `Counted`'s entry induction-variable load the value zero; assertion propagation replaces it and reverses the comparison. Managed VN and assertion phases are still stubs, so equal-sized code retains a register comparison. | Complete VN and assertion-propagation dependencies rather than special-casing this loop or changing inversion. | Memory-PHI allocation/query and memory-loop provenance are implemented. Full VN map selection, traversal/evaluation and assertion propagation remain. |
| B268 | Dump divergence / clone numbering | The inverted `Counted` entry condition has matching tree shape but different clone `TreeId` ordering in native and managed dumps. | Reconcile full expression-clone allocation/numbering order; do not renumber the sample independently. | Open. Does not block execution, but is not an accepted diagnostic difference. |
| B269 | Port bug / object guard operand | `ObjectAllocator.IsGuard` used binary `AsOp` on unary `GT_JTRUE`, asserting for valid guard trees. | Read the unary operand without weakening type assertions. | Fixed with `AsUnOp`; four previously failing Debug guard cases now pass in the combined object selection. |
| B270 | Port bug / cloned self-copy identity | Self-copy elimination allocated a new NOP root instead of applying native's base-field-only NOP transformation to the cloned root. | Retain the existing root, aliases and logical ID with `BashToNOP`. | Fixed with an identity-sensitive regression, not just an operator check. |
| B271 | Port bug / hardware import classification | The table-driven predicate checked only `HW_Flag_SpecialImport`, omitting `HW_Flag_InvalidNodeId` from native `HasSpecialImport`. Portable `Vector128.op_Addition` consequently reached an invalid instruction-table entry. | Preserve both flags so non-node portable operations reach their special importer. | Fixed. The focused regression fails with the original predicate and passes with the corrected mask. The previously failing native-host `FoldHardware` now executes and emits the same four bytes as native. |
| B272 | Required port / GS phase | `gsPhase` was a no-op even when stack buffers required GS layout. `ByrefLoadStore` reached final frame layout with `compGSReorderStackLayout` set but `lvaGSSecurityCookie` still invalid, asserting in `lvaGetDesc`. | Port complete native GS initialization and parameter-shadowing orchestration, not just allocation of a cookie local. | Complete Windows-AMD64 phase with focused shadow/cookie coverage. The full eight-method hardware corpus executes, including optimized and minopts GCStress4 runs. Native byte parity is not established for the stack-buffer method. |
| B273 | Required port / stack-array expansion | `fgExpandStackArrayAllocations` was a no-op, leaving the synthetic `StackArrayLocal` argument on a heap-allocation helper. Activating object stack allocation then reached call lowering with an argument having no ABI segments. | Expand every stack-array helper into method-table/length initialization and its local address, preserving split-tree evaluation order and repeated expansions. | Implemented with the required statement block-operation morphing helper. The real object corpus now executes class and array stack allocations; focused expansion fixtures and final packet integration are pending. |

Larger module decomposition and broad unit-test infrastructure belong to the
post-port phase. Add concrete candidates here as evidence appears, rather than
preemptively planning a redesign of every compiler subsystem.
